# Exceções do gate de SCA (ADR-0025).
# Toda entrada exige `reason` com justificativa, data e critério de remoção.
# Nunca desligar o gate: exceção sem reason é rejeitada em code review.
#
# Nenhuma exceção ativa no momento.
#
# A exceção de GHSA-5jmj-h7xm-6q6v (jackson mass-assignment) foi REMOVIDA em 2026-07-21: o OSV
# revisou o advisory novamente e ele não casa mais com nenhum pacote do SBOM (osv-scanner reporta
# "unused ignore"). Como o osv-scanner-action v2.3.8 do CI FALHA (exit 1) em ignore não-usada, uma
# exceção obsoleta passa a bloquear o gate — daí a remoção. Se o OSV re-listar o advisory contra uma
# versão resolvida, reintroduzir a exceção (com reason atualizada) ou fazer o bump correspondente.
