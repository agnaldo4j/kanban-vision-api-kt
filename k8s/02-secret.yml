apiVersion: v1
kind: Secret
metadata:
  name: kanban-vision-secret
  namespace: kanban-vision
  labels:
    app.kubernetes.io/name: kanban-vision-api
type: Opaque
# Values are base64-encoded placeholders — replace before applying to production.
# Generate with: echo -n 'your-value' | base64
stringData:
  DATABASE_PASSWORD: "CHANGE_ME"
  JWT_SECRET: "CHANGE_ME"
