# Lessons Learned — the review-to-skills feedback loop

Append-only log of **durable, generalizable** lessons surfaced by a PR/gate — the ones worth changing
the way we work so the same mistake does not recur. It is the durable half of the loop the pr-harness
rubric §6 and the `/pr-review` skill drive: a review surfaces a lesson → it is **recorded here**, in the PR
that learned it, and — when it warrants — **applied** to a skill/rule/rubric in that same PR.

**What belongs here:** a recurring miss, a false-negative from a gate/reviewer, a subtle trap that cost
time, a process gap. One entry per lesson. Keep it terse: what happened, the durable rule, where it was
applied. **What does NOT belong:** feature-specific findings (those live in the ADR / the gap's notes),
one-off nits, or lessons without a concrete "apply it here."

> 🔴 **A lição entra no PR que a aprendeu — junto do código, enquanto o contexto está fresco.** Esse PR já
> está em revisão, então registrar a lição nele custa **zero ciclo extra**. Não há fila, não há lote, não há
> PR de processo separado: os três existiram entre 2026-07-28 e 2026-07-30 e a maquinaria nunca disparou uma
> única vez — o contador se auto-zerava e a fila fragmentou em dois lugares. Removida no #390.
>
> **O `post-merge-harvester` faz fechamento e nada mais.** Se a revisão ensinou algo que não foi escrito no
> próprio PR, ele **relata** ao mantenedor; não abre PR, não enfileira. Quem decide o que vira mudança de
> processo é o mantenedor — sempre, e antes da ação.

**Anti-loop guard (why this doesn't spiral):** the harvester harvests **only after a *real implementation***
— a merged PR that touches production source (`*/src/main/**`). A **process/doc/skill/ADR/test-only PR gets
closure only, no harvest**, so an improvement-PR never spawns another improvement-PR (the loop terminates in
one level). This file itself is process — merging it triggers closure only.

| Date | PR(s) | Lesson (the durable rule) | Applied to |
|---|---|---|---|
| 2026-07-21 | #323 #324 #325 | **Read the raw inline review comments, never trust the harness *summary*.** The pr-harness report can say "APPROVE / low severity" while its own inline comments (and Codex) carry P1s — it happened 3× in one cycle, including a *blocking* supply-chain P1. Verify via `gh api .../pulls/N/comments` + the GraphQL `reviewThreads` (`isResolved`), not the prose summary. | `.claude/skills/pr-review` (hard-rule), `feedback_always_run_pr_review` memory |
| 2026-07-21 | #325 | **CI already runs pr-review — read the PR's posted reports; don't re-dispatch the harness by default.** `pr-review.yml` (GAP-CT) posts the `[claude]` report after CI. Manual `/pr-review` is the exception (CI hasn't run yet / immediate feedback). Redundant *and* risky — see next row. | `.claude/skills/pr-review` (default at top) |
| 2026-07-21 | #325 | **The pr-harness subagent runs Bash in the *same* working directory and can `git checkout`.** A manual dispatch switched the branch and reverted files mid-review (the pushed commit was intact — it reviews the remote SHA). If dispatching manually, re-check `git branch --show-current` afterwards. Preferring CI reports avoids this entirely. | `.claude/skills/pr-review` (manual-dispatch caveat) |
| 2026-07-21 | #325 | **Align a transitive dependency family for a CVE via its BOM, not per-module pins.** A per-module Netty pin missed `netty-resolver-dns`/`netty-codec-dns` (pulled at a CVE'd 4.1.x by Lettuce) → the blocking `supply-chain` gate went red. `platform("io.netty:netty-bom:<v>")` aligns the whole family in one line. | `.claude/rules/stack.md` |
| 2026-07-21 | #325 | **Kotlin trap — `/*` (e.g. inside a `` `glob/**` ``) within a KDoc opens a *nested* block comment** (Kotlin supports nesting) that never closes → "Unclosed comment". Avoid `/*` sequences in doc text (write `pkg.sub`, not `` `pkg/sub/**` ``). *(Correction, #326: this — not explicit type args like `evalsha<List<Long>>`, which are valid Kotlin — was the real cause of that build's error. A first draft of this log misdiagnosed it; Codex caught it. Lesson-about-lessons: verify the mechanism before recording it.)* | `.claude/rules/kotlin-quality.md` (pitfalls) |
| 2026-07-21 | GAP-CZ | **A lesson log is not enough — the loop must be *applied and automated*.** Documenting lessons in a file still relies on someone acting on them. Encoded the loop as the `post-merge-harvester` agent (closure + harvest + *apply* to skills/rules as a ready process-PR), triggered whenever the author says a PR merged. | `.claude/agents/post-merge-harvester.md`, `.claude/rules/workflow.md` (After-PR-Merge) |
| 2026-07-21 | #324 | **Verify a library's internals from its source, not from assumption.** Ktor's `DefaultRateLimiter` turned out to be a *fixed-window* counter, not a token bucket — refuting a reviewer P2 and producing ADR-0042. A five-minute read of the jar source settled a claim two reviewers got wrong. | `feedback_full_rescore_verify_code` memory (reinforced) |
| 2026-07-23 | #329 #331 | **A feature PR's `supply-chain` gate can go red on deps it never touched — a fresh OSV disclosure is repo-wide.** Twice in one session (jackson-3 via Flyway 12/logstash 9; a Netty batch) a k8s-only PR was blocked by a new CVE on an already-pinned transitive family. Don't fold the fix into the feature — it's a separate `fix/` PR (bump the family via its BOM). The aggregate SBOM covers each module's `runtimeClasspath`, so repeat the BOM in **every module** that resolves the family. *(Update: **GAP-DA (#TBD) added `migrationRuntime` to `includeConfigs`**, so the migration binary's classpath is now in the SBOM too — before it, a migration-only dep was a blind spot you had to check by hand via `:mod:dependencies --configuration migrationRuntime`.)* Also: the CI `osv-scanner-action@v2.3.8` **fails on an *unused* ignore** (local 2.4.0 only warns), so a stale `osv-scanner.toml` exception blocks the gate by itself. *(Codex P2 on #332 corrected an earlier draft that wrongly implied the SBOM scans `migrationRuntime` — same "verify the mechanism" trap as the #326 row.)* | `.claude/skills/github-ci-health` (§4 signature + §7 pitfall), `project_osv_scanner_ci_gotchas` memory |
| 2026-07-23 | #330 | **Migrating a static-target Prometheus alert to k8s pod SD changes `up == 0` semantics.** With `kubernetes_sd_configs` role `pod`, a *total* outage removes every `up{job=...}` series, so `up == 0` evaluates an empty vector and never fires. Add an `absent(up{job=...})` companion (critical) for "zero targets"; the `up == 0` rule still covers partial outage. Reviewer (Codex) caught it as P1. | `.claude/skills/opentelemetry` (ServiceDown callout) |
| 2026-07-23 | #330 | **A `subPath` ConfigMap mount freezes the file at pod-start — even `/-/reload` rereads the stale copy.** Kubelet only propagates ConfigMap updates to *directory* mounts (symlink swap). Mount config as a directory (no `subPath`) and point the config flag there (or add a reloader sidecar); otherwise a config change needs `kubectl rollout restart`. Reviewer (Codex) caught it as P2. | `.claude/skills/local-and-production-environment` (deployment.yml callout) |
| 2026-07-23 | #333 #335 | **A copied config diverges from its source under k8s — reused compose configs need per-environment label adaptation.** Container-metric selectors (`{name="kanban-vision-app"}` in the Grafana dashboard; `{name!=""}` in rules) use the *compose* cAdvisor label `name`, absent under the k8s kubelet (which emits `namespace`/`pod`/`container`) → panels/alerts silently see no data. Two reviews (Codex P2×2) caught the dashboard and the Alertmanager `inhibit_rules` key. Adapt every copied config's label selectors to the target environment; track the compose↔k8s duplication (GAP-DB). | `.claude/skills/opentelemetry` (scope note) |
| 2026-07-23 | #335 | **An Alertmanager `inhibit_rule` `equal` key only works if EVERY source/target alert emits those labels — and `sum by (...)`/`histogram_quantile` silently drop labels outside the `by` clause.** A first fix keyed inhibition on `['namespace','pod']`, but `HighHttpErrorRate`/`HighHttpLatencyP95` aggregated by `instance` only → those alerts lost `namespace`/`pod` and a critical on one pod inhibited warnings on every pod. Fix: preserve the key labels in the aggregations (`sum by (namespace, pod, instance)`). Verify each alert's `expr` emits the key labels; aggregated label-less alerts (`sum`/`max`/`absent`) match by "absent-on-both = equal" — a conscious residual. Reviewer (Codex) caught it as P2. | `.claude/skills/opentelemetry` (inhibit_rules callout) |
| 2026-07-23 | #341 | **Reusing a cycle/graph detector across gates requires re-validating what a self-loop/self-ref *means* in that domain.** The new DI-wiring cycle gate copied `ClassCycleTest`'s "drop self-ref" filter (`it != impl`) — correct for *data composition* (`TreeNode(children: List<TreeNode>)`, legitimate) but wrong for *eager DI construction* (self-injection `single { A(get()) }` / `class A(other: A)` → real Koin `StackOverflowError`). The inherited filter blinded the new gate to the simplest cycle it promised to catch (Codex + harness **P2**). Also (harness **P3**): a `associate { it.name to … }` lookup by simple name silently collapses homonyms last-wins — fail loud on collision instead (the reason `ClassCycleTest` uses FQN, GAP-CV). | `.claude/agents/pr-harness.md` (§2.5 checklist), `.claude/skills/circular-dependency-control` (automated-detection note) |
| 2026-07-24 | #345 | **A *cancelled* `pitestAll` run reports `0 mutants`/`0%` for the not-yet-executed modules — a false ❌, not a mutation regression.** `pitestAll` declares all 6 module tasks via `dependsOn` (unordered) with `org.gradle.parallel=true`, so a cancelled in-flight run leaves **whichever** modules hadn't finished at `0 mutants / 0%` — non-deterministic (here it was `usecases`/`http_api`, but they are **not** guaranteed to run "last"/serially, contra a first draft of this note; Codex P2 on #346). The tell is **0 mutants generated** (a real coverage drop keeps the mutants, only SURVIVED rises). Check the run status (`gh run view <ID> --json conclusion` → `cancelled`) and reproduce locally (`:usecases:pitest` → 311 mutations, 63% ≥ 55%) before treating it as a regression. | `.claude/skills/github-ci-health` (§4 signature) |
| 2026-07-23 | #338 | **A foreground command in a CI step with no timeout can hang the whole job until the runner's 6h limit — no report, no teardown.** GAP-DA's migrate-binary smoke probe (`docker run`, not `-d`) would hang forever if the native binary deadlocked in a DB/native-init path — ironically the exact regression the probe exists to catch. Bound the command (`timeout <s> docker run …` → a hang becomes a prompt exit-124 gate failure; force `docker rm -f` cleanup) AND add `timeout-minutes:` to the job as a safety net (no `ci.yml` job had one before #338). Reviewer (Codex) caught it as P2. | `.claude/skills/github-ci-health` (§7 pitfall) |
| 2026-07-24 | #350 | **Unwrapping a guaranteed-`Right` under a pre-guard is a *dead branch* that quietly lowers coverage.** When a caller pre-guards the exact condition an `Either`-returning op raises on (an aggregate invariant mirrored at the call site), `op().getOrNull() ?: return` is an *uncoverable* branch — JaCoCo drops even above the gate and no test can kill it. Absorb the impossible `Left` inside Arrow with `.onRight { }` (caller stays total, identical behaviour, no dead branch) and pin the totality assumption ("under a satisfied pre-guard, never `Left`") as a **test**, not a comment. Applied mid-PR on a harness suggestion (no persisted `[claude]`/Codex thread — Copilot was quota-limited, so the signal came from the second commit + the diff). | `.claude/rules/kotlin-quality.md` (pitfalls) |
| 2026-07-24 | #353 | **A default-valued parameter that reads an effect (`= Instant.now()`, `= UUID.randomUUID()`) is a *hidden* clock/RNG — the signature looks edge-injectable but the no-arg path stays impure.** GAP-DK made `SimulationEngine.runDay`'s **clock path** referentially transparent (`now` became an input, not a hidden effect — the whole function is not yet RT, see the corollary) by injecting `now: Instant` from the edge (a Koin `Clock`) and **removing** the `= Instant.now()` defaults on `DomainEvent.occurredAt` and `Audit.now/touch` — making the hidden clock illegal-state-unrepresentable. Corollary: injecting *one* effect (clock) doesn't make a function deterministic while *another* remains (`UUID.randomUUID` on `Movement`/`DailySnapshot`/`FlowMetrics` IDs — a separate identity concern, GAP-DJ/DL); verify the whole effect set and scope the test claim to exactly what became pure. The general "effects at the edge" principle was already in the skill; this is the non-obvious default-argument corollary. | `.claude/skills/fp-oo-kotlin` (Sinais de Alerta + checklist) |
| 2026-07-24 | #353 | **A signature-change ripple can be absorbed with a *test-only* lower-arity overload delegating to the new member — safe only under two conditions.** GAP-DK's `runDay` gained `now`; a test `internal` 3-arg overload delegating to the 4-arg member with `Instant.EPOCH` kept ~55 behavior call-sites untouched. Footgun unless: (1) the overload has **distinct** (lower) arity — with the *same* arity the production **member wins by precedence**, so the overload is silently shadowed and never intercepts (dead, not recursive); and (2) the **full-arity member is exercised directly** in a dedicated test so the new parameter is actually covered. Keep it in `*TestSupport.kt`, never `src/main`. Harness rated the pattern acceptable-and-documented; recorded as reusable guidance since signature-changing refactors recur across the `/fp-oo-kotlin` arc. | `.claude/rules/testing.md` (Known Pitfalls) |
| 2026-07-24 | #348–#350 | **A type in an exported signature must reach each consumer's compile classpath — `api` when reached transitively, `implementation` when every consumer already depends on the source directly.** After ADR-0044 the domain aggregates return `Either<KanbanError, _>`; `arrow-core` was `implementation`, hiding `Either` from consumers that reach it *through* `domain-kanban` → `api` (as `usecases` already does). Caveat (Codex P2 on #352): the *project* deps stay `implementation` on purpose even though `Board.audit: Audit`/`Scenario.board: Board` are cross-module public — every consumer declares the domain modules directly (`ProjectDependencyGraphTest` asserts the edges), so nothing is transitive. Operative rule, not "all exported-type deps are `api`". | `.claude/rules/architecture.md` (Known Pitfalls) |
| 2026-07-25 | #355 | **Refining a *persisted/serialized* field's type with a value-class smart constructor makes legacy data flow into its `require` — one invalid historical value makes the whole aggregate un-loadable.** GAP-DH's `NonBlankTitle` on `Decision.AddItem.title` was safe on the *new* input edge (DTO + `Card.init`), but `Decision.AddItem` had **no** guard before this PR, so a pre-GAP-DH blob could hold `ADD_ITEM {"title":""}` in `stateJson` (the simulation's `decisions` — not `DailySnapshot`, which serializes only metrics/movements). The decode (`DecisionSurrogate.toDomain`) threw straight into `NonBlankTitle(...)` → `Either.catch` → `PersistenceError` → `findById`/`findAll` of the **entire simulation** 500s, not just the field. The new entry edge is easy to remember; the **decode of persisted history** is the blind spot. Fix (`de596cf`): `decodeTitle()` coerces legacy blank to a `"(untitled)"` sentinel + a legacy-decode test — no data migration, history stays readable. Codex rated P1, this harness P2 — same finding. | `.claude/skills/fp-oo-kotlin` (value-class-on-serialized-field callout + Sinais de Alerta row), `.claude/rules/migrations.md` (legacy-tolerant decode section), `.claude/agents/pr-harness.md` (§2.5 checklist) |
| 2026-07-25 | #359 | **Um skill/doc que cita API do domínio em tempo presente deve ter cada identificador grep-verificado no SHA — nome exato, singular vs plural, membro vs extensão.** O enriquecimento OOD do `/fp-oo-kotlin` ensinava `simulation.advanceDay().appendDecisions(decisions)…`, mas só existe `appendDecision` (singular); e mostrava `Card.block` como extensão quando é método membro. Uma diferença de um caractere "compila na cabeça" do autor/revisor e gera exemplo não-compilável — a leitura de prosa não pega. Harness P2/P3 no #359. | `.claude/agents/pr-harness.md` (§2.5 — bullet skill/doc) |
| 2026-07-25 | #360 | **Um refactor tell-don't-ask "behavior-preserving" que troca um append de LOTE por um `fold` do append SINGULAR troca O(n) por O(n²) em silêncio.** Ao mover a transição de lote do call-site para dentro do agregado, o engine passou a dobrar `appendDecision` (singular) num `fold` — cada iteração recopiava a lista de decisões acumulada (O(atual×lote + lote²)), regredindo o antigo append de lote único; comportamento idêntico, testes verdes. Regra: ao encapsular, dê ao agregado um método de **lote** (`appendDecisions(List)` = uma concatenação), não itere o singular. Ironia colhida: a própria seção OOD do `/fp-oo-kotlin` (adicionada no #359) exibia o padrão `fold(appendDecision)` como exemplo ✅ — corrigido junto. Codex P2 no #360; corrigido com `Simulation.appendDecisions`. | `.claude/skills/fp-oo-kotlin` (exemplo Tell-Don't-Ask + callout de complexidade), `.claude/agents/pr-harness.md` (§2.5 checklist) |
| 2026-07-25 | #364 | ⚠️ **[CORRIGIDA — ver a linha `#364–#369` no fim: o `is_error` era falta de SALDO na API, não defeito; GAP-EK foi fechado.]** **O gate de revisão pode ficar silenciosamente VAZIO — ausência de parecer não é aprovação.** O #364 (implementação real, `src/main`) foi mergeado com **zero revisão** e nada vermelho: Codex não comentou, Copilot recusou por quota e o harness **rodou e falhou**. O run resolveu o PR certo (`Resolved PR #364 — head … d976afa7`) e a `claude-code-action` retornou `is_error: true` com `num_turns: 1`, `duration_ms: 301`, `total_cost_usd: 0` — a mesma assinatura do GAP-CW, agora já com `--model claude-sonnet-5` fixado (logo, causa-raiz distinta: acesso/validade do modelo, não o sufixo `[1m]`). Como o passo é `continue-on-error` (advisory por design), o run concluiu **verde** e o único sinal foi um `::warning` na aba Actions — nenhum comentário no PR. Família do "✅ fabricado" do GAP-CC: silêncio lido como aprovação. Regra: **antes de dar o PR como pronto, confirmar que o parecer EXISTE para o head SHA**; se não existe, checar o run por `is_error` e dispatchar manual. Armadilha de diagnóstico: em `workflow_run` o `head_sha` exposto para os runs de `pr-review.yml` é o da **default branch**, então filtrar por SHA do PR dá falso "nunca rodou" — case por horário e confirme `Resolved PR #<n>` no log. Visibilidade no PR + causa-raiz do `is_error` = **GAP-EK** (mudança de gate/CI, não emendável aqui). **Não é pontual:** o mesmo `is_error: true` aparece nos runs de #363, #364 e #365 — o gate está vazio há pelo menos três PRs. | `.claude/skills/pr-review/SKILL.md` (callout "ausência de parecer não é aprovação" + verificação), Board #6 `GAP-EK [N]` |
| 2026-07-25 | #365 | **"Existe um parecer no PR" não é o mesmo que "existe um parecer PARA ESTE head" — sem SHA no report, um parecer stale faz o head novo passar por revisado.** A verificação escrita no #364 lia o head SHA e nunca o usava: contava comentários `## PR Review Harness` no PR inteiro, então num PR que recebeu novo push depois de revisado o contador seguia > 0 e declarava revisado um commit que ninguém olhou — o próprio falso-verde que o callout existia para impedir, um nível acima. Os **inline** já ancoravam no SHA (`<!-- pr-harness:<sha>:… -->`); o **report-resumo** não tinha marcador nenhum. Fix: o rubric passa a abrir o report com `<!-- pr-harness-report:${REVIEWED} -->` e a verificação filtra por esse marcador. Pegadinha de ferramenta encontrada ao validar: `gh api --jq` aceita **um** argumento — `--arg` falha com `accepts 1 arg(s), received 4`; interpole o SHA na expressão. Codex P2. | `.claude/agents/pr-harness.md` (§5 — marcador de SHA no report), `.claude/skills/pr-review/SKILL.md` (filtro por SHA + nota do `--arg`) |
| 2026-07-25 | #365 | **Um formato exigido pelo rubric só vale se TODOS os caminhos de emissão o exigirem — o prompt do CI é um segundo autor do artefato.** A correção acima passou a mandar o report abrir com `<!-- pr-harness-report:<sha> -->` no `.claude/agents/pr-harness.md`, mas o prompt do `.github/workflows/pr-review.yml` continuava pedindo só *"começando com o título exato `## PR Review Harness`"*. Como o CI emite quase todo parecer, a verificação daria falso "sem parecer" **justamente no caminho default** — e um check que erra sempre volta a ser ignorado, que é o mecanismo do #364 um nível acima. Regra: ao mudar o formato de um artefato produzido por agente, altere o rubric **e** todo prompt/workflow que o instrui, no mesmo PR. | `.github/workflows/pr-review.yml` (prompt exige o marcador), `.claude/agents/pr-harness.md` (§5 — ordem explícita + ponteiro recíproco ao workflow) |
| 2026-07-25 | #365 | ⚠️ **[CORRIGIDA — ver a linha `#364–#369` no fim: o `is_error` era falta de SALDO na API, não defeito; GAP-EK foi fechado.]** **Ao verificar "o gate rodou?", enumere TODOS os estados terminais — com `continue-on-error` há três, e dois concluem verde.** O grep de diagnóstico cobria parecer-postado e `is_error`, mas não o run que **nem chega** ao harness (`Nenhum PR aberto com head == $RUN_SHA … Pulando`, ou job `skipped`) — verificado no run `30171786595`. Pior: `gh run view --json jobs` reporta o passo como `conclusion: success` mesmo com `is_error: true`, então a checagem "barata" por JSON reintroduz o falso-verde que o bloco existia para matar — só o `--log` distingue. Mesma família do "✅ fabricado" (GAP-CC) e do guard de `gh api` vazio. | `.claude/skills/pr-review/SKILL.md` (três estados + proibição do atalho por JSON) |
| 2026-07-25 | #367 | **Um predicado emprestado carrega a direção-segura do problema ORIGINAL — reusá-lo noutra decisão inverte o fail-safe.** A regra de "pode mergear sem revisão?" foi escrita reusando o `*/src/main/**` do guard anti-loop do `post-merge-harvester`. Lá, errar para "não é implementação" custa uma lição não colhida (barato); aqui, custa **mergear sem revisão** um PR que muda produção. E a exclusão vaza justamente o que não tem `src/main`: `Dockerfile`, `k8s/**`, `.github/workflows/**`, `build.gradle.kts`, `buildSrc/**`, `config/**`, `scripts/**` (consumidos pelos gates), migration Flyway. O #367 **se auto-isentaria** ao alterar `.github/workflows/pr-review.yml`. Regra: decisão de risco usa **allowlist** do que é dispensável, nunca blocklist do que é arriscado — o caminho não-reconhecido tem de cair no lado seguro. E ao emprestar um predicado, verifique se a direção-segura dele coincide com a do novo uso. Codex P2. | `.claude/skills/pr-review/SKILL.md` (allowlist executável + porquê), `.claude/agents/post-merge-harvester.md` (aviso de não-reuso do predicado) |
| 2026-07-25 | #367 | **Um snippet de DECISÃO dentro de uma skill é código de gate — `cmd \| grep -v …` engole exit≠0 e entrega stdout vazio.** A allowlist recém-criada (linha acima) lia `gh pr view … \| grep -vE …`; com token expirado, rate limit ou PR inexistente o `gh` devolve **stdout vazio e exit 1**, indistinguível de "doc-only" — e como vazio significava "isento", a regra criada para impedir merge sem revisão passava a **autorizá-lo**. Modo `gh api` exit-0-corpo-vazio do GAP-CC/#288, agora dentro do próprio guard. Regra: capturar em variável, tratar o `||`, e distinguir **vazio** de **falhou** (vazio ⇒ indeterminado ⇒ lado seguro). | `.claude/skills/pr-review/SKILL.md` (comando endurecido), `.claude/agents/pr-harness.md` (§2.5 — snippet de decisão em skill conta como gate) |
| 2026-07-25 | #367 | **Allowlist por diretório vaza; a granularidade tem de ser o TIPO de arquivo.** `\.claude/` inteiro isentava `hooks/guard-security.sh` (o guard de segredo hardcoded que a própria `security.md` declara) e `settings.json`; `[^/]+/src/test/` isentava `architecture/src/test/**` — o módulo **test-only** das 15 fitness functions, onde apagar `ProjectDependencyGraphTest.kt` deixa `testAll`/JaCoCo/PITest **verdes** porque não há `src/main` para cobrir. Fix: só `**/*.md` nesses diretórios e **enumerar** os módulos de produto cujo `src/test` é isento, deixando `architecture` fora. Verificado caso a caso antes de commitar. Harness P2. | `.claude/skills/pr-review/SKILL.md` (allowlist por tipo) |
| 2026-07-25 | #368 | **Um guard cuja allowlist inclui os arquivos que o definem autoriza a própria remoção.** A allowlist isentava `.claude/**/*.md` inteiro — ou seja, `skills/pr-review/SKILL.md` (a decisão de isenção), `agents/pr-harness.md` (o rubric), `rules/security.md` (o que é proibido) e `docs/politicas-explicitas.md` (quality gates, regras de ADR). Um PR que reabrisse o pipe fail-open ou afrouxasse o rubric sairia como "doc puro" e mergearia sem revisão. Não é hipotético: aplicada a deny-list, o **#365 passa de EXEMPT a REVIEW-REQUIRED** — e ele de fato mergeou sem revisão alterando o rubric. Regra: ao escrever um guard, rode-o **contra o próprio diff que o introduz e contra os arquivos que o definem**; se ele se isenta, está errado. Codex P1. | `scripts/review-exemption.sh` (deny-list `GATE`), `.claude/skills/pr-review/SKILL.md` |
| 2026-07-25 | #368 | **Snippet canônico com variável nunca atribuída publica vazio (ou aborta sob `nounset`).** O §5.5 mandava `printf '%s\n' "$PARECER"`, mas `PARECER` não era definido em lugar nenhum do rubric: o comando postaria **só o marcador de SHA**, sem parecer — falha silenciosa no exato artefato que a sessão passou a exigir. Corrigido para heredoc **com aspas** (`<<'EOF'`), que também protege crases/`$`/`!` do markdown do próprio parecer. Regra: snippet canônico é executado por quem lê — toda variável usada tem de ser atribuída **no snippet**, e ele deve ser rodado antes de virar cânone. Codex P2. | `.claude/agents/pr-harness.md` (§5.5 — heredoc citado) |
| 2026-07-25 | #368 | **Enumerar "os subdiretórios perigosos" vaza; proteja o diretório inteiro.** A primeira deny-list listava `.claude/{agents,rules,skills/pr-review}` — e deixou de fora `CLAUDE.md`, a skill `/github-ci-health` (que documenta o próprio sintoma de gate vazio) e `/xp-kanban`, para a qual a regra *protegida* `rules/workflow.md` **delega** o Board Protocol: bypass por indireção, o caso mais sutil. O PR que introduziu a deny-list alterava `/github-ci-health` e o próprio script o chamava de EXEMPT. Correção: `.claude/**` e `adr/**` saem da allowlist inteiros — skill nova entra protegida por padrão, em vez de depender de alguém lembrar de adicioná-la. Regra geral: numa deny-list de segurança, o default do diretório é *proteger*; a exceção é que se enumera. Harness P2. | `scripts/review-exemption.sh`, `.claude/skills/pr-review/SKILL.md` |
| 2026-07-25 | #368 | **`mktemp -t <prefixo>` é BSD; no GNU coreutils aborta — e o runner é `ubuntu-latest`.** O snippet canônico do §5.5 (o mesmo que já tinha o bug do `$PARECER`) usava `mktemp -t pr-harness-report`: no macOS funciona, no runner dá `too few X's in template` → `$BODY` vazio → redirect falha → **parecer não postado**, com o run verde por `continue-on-error`. Dois defeitos no mesmo snippet em rodadas seguidas mostram o padrão: **snippet canônico tem de ser executado no ambiente-alvo**, não só lido — e "funciona na minha máquina" é especialmente traiçoeiro em utilitário POSIX com divergência BSD/GNU. Fix: `mktemp "${TMPDIR:-/tmp}/pr-harness-report.XXXXXX"`. Harness P2. | `.claude/agents/pr-harness.md` (§5.5) |
| 2026-07-25 | #366 | **Um branch `is` como ÚLTIMO arm de um `when` exaustivo sobre sealed é *partial* — o caminho falso do type-check é inalcançável.** Mesma família do dead branch do #350, mecanismo distinto: o último `is X ->` ainda emite o check, mas o tipo garante que o valor já casou antes, então nenhum teste fecha o arm falso — JaCoCo marca partial e o `codecov/patch` reprova um diff totalmente testado (2 partials novos em `SimulationEngine`/`RunDayUseCase`). Fix: a variante inerte vira o **primeiro** branch — ambos os caminhos passam a ser exercitados, semântica idêntica por ser no-op, com comentário no código para ninguém "arrumar" movendo de volta ao fim. Aparece na conversão **enum → sum type** (enum vira switch e não mostra partial), o que a fila de cards torna iminente (GAP-DL `Movement`/`ExecutionResult`, GAP-DI `DecisionRequest`). Corolário de método: **atribua partials medindo base-vs-head** — a main já tinha 1 pré-existente e só o delta era do PR. Contexto do sinal: Codex e harness não comentaram e o Copilot recusou por quota, então quem pegou foi o `codecov/patch` — gate não-humano. | `.claude/rules/kotlin-quality.md` (pitfalls) |
| 2026-07-25 | #366 | **Decode tolerante que DESCARTA o registro ilegível não é degradação de leitura — é deleção, porque o repo re-serializa o agregado inteiro.** A regra de legado existente cobria *valor inválido em dado antigo* (compat para trás); este é o eixo oposto: **variante/tag que este release não conhece**, e o gatilho realista **não é corrupção, é rollback** — um release grava uma 5ª tag, reverte-se, e os pods antigos não leem o histórico que acabaram de gravar (sob rolling update as versões convivem, então blob misto é o caso normal). Preservar em `Decision.Unknown(type, payload)`/`MovementType.Unknown(raw)` mantém o round-trip byte-idêntico; um `skip` viraria **deleção permanente no próximo save**, já que `JdbcSimulationRepository` faz `it[stateJson] = encode(simulation)` do agregado todo — perda por um caminho que ninguém lê como escrita (vale p/ qualquer blob read-modify-write). Custo da tolerância: o `else` que explodia era o que pegava decoder desatualizado, então uma variante nova passa a virar `Unknown` **em silêncio** — como a tag é `String` o compilador não dá exaustividade no decode, e a garantia tem de ser reposta por um round-trip exhaustiveness test que **para de compilar** ao surgir variante nova. Assimetria a preservar: persistido degrada, **input de cliente segue fail-closed (400)** — não propagar a tolerância para a borda HTTP. | `.claude/rules/migrations.md` (nova seção "Variante (tag) desconhecida no blob") |
| 2026-07-25 | #369 | **"Preserva o registro" é uma afirmação sobre o TIPO do campo portador, não sobre a intenção — e o tipo tipado mente.** A regra recém-escrita dizia que `Decision.Unknown(type, payload)` "volta idêntica ao wire". Medido contra o código mergeado: `payload` é `Map<String,String>`, então um payload futuro com número/boolean/array/objeto lança `JsonDecodingException` **antes** de `Decision.Unknown` existir (= o 500 que a tolerância existia para evitar), e um campo extra no topo da decisão é descartado em silêncio por `ignoreUnknownKeys` (= some no próximo save). Ou seja, a tolerância cobria o eixo *tag desconhecida* e não o eixo *forma do payload*. Regra: preservação real exige reter o **JSON cru** (`JsonElement`), não um mapa tipado; se o carrier tipado bastar, **declare o escopo** ("payload plano de strings") em vez de prometer identidade de wire, e teste os dois casos — que são invisíveis porque só ocorrem com dado *futuro*. Meta-lição: eu escrevi a regra no mesmo ciclo em que implementei o código e **não medi o carrier**; o log já registra duas vezes (#326, #332) o custo de gravar mecanismo mal diagnosticado. Codex P2; lacuna de código cardada como **GAP-EN**. | `.claude/rules/migrations.md` (tabela do que o carrier tipado NÃO preserva), Board #6 `GAP-EN [M]` |
| 2026-07-25 | #369 | **Gravei o MECANISMO errado de novo — e desta vez o certo só apareceu compilando.** A regra dizia "um branch `is` como último arm de `when` exaustivo é scored partial". O harness compilou cada forma em Kotlin 2.4.10 sob JaCoCo e leu os contadores: `is D.Unknown -> Unit` (corpo **vazio**) por último = PARTIAL, mas `is D.Add -> out += "a"` e `is D.Unknown -> out.size` (corpos **não-vazios**) por último = FULL, com o caminho falso igualmente inalcançável. O gatilho é **corpo vazio no último arm**, não o `is`: o corpo vazio deixa o compilador inverter o salto e o fall-through cai no `NoWhenBranchMatchedException`, que o `KotlinWhenFilter` do JaCoCo só reconhece como *alvo*. **Consequência prática:** a prescrição "ordene a inerte primeiro" só vale com **uma** variante inerte — com duas, a segunda volta a ficar por último com corpo vazio e o partial retorna, com o autor tendo seguido a regra. A regra correta é "nenhum arm de corpo vazio pode ser o último". Este é o **terceiro** incidente da mesma forma no log (#326, #332): mecanismo afirmado por leitura, refutado por execução — e nas três vezes quem mediu foi o revisor, não o autor. | `.claude/rules/kotlin-quality.md` (mecanismo medido + tabela + prescrição corrigida) |
| 2026-07-25 | #364–#369 | **CORREÇÃO das linhas de #364 e #365 — o harness sem parecer NÃO era defeito: era falta de SALDO na API.** Aquelas linhas descrevem o `is_error: true` como falha a diagnosticar e apontam para o card GAP-EK ("causa-raiz do `is_error`", "visibilidade no PR"). O mantenedor esclareceu (2026-07-25) que a `claude-code-action` é **paga**: sem saldo não roda, com saldo volta a rodar — *"não tem nada a modificar"*. **GAP-EK fechado.** O que permanece verdadeiro daquelas linhas: ausência de parecer não é aprovação, e o run conclui verde de qualquer forma — então não se registra "harness APPROVE" sem ler um parecer real. O que era falso: tratar isso como incidente. **A meta-lição é sobre mim:** eu tinha quatro PRs seguidos sem parecer, o `--model` já corrigido e um run verde — e li tudo como confirmação de bug, sem nunca considerar a explicação mais barata (custo). Escrevi o diagnóstico errado em **três** documentos e abri um card. Regra: **antes de investigar a ausência de um serviço pago, verifique se ele simplesmente não foi pago.** Aplicado também como anti-diagnóstico em `/github-ci-health` §5, para a próxima sessão não reabrir a caça. | `.claude/skills/pr-review/SKILL.md`, `.claude/skills/github-ci-health/SKILL.md` (§4 + §5 anti-diagnóstico), Board #6 `GAP-EK` → Done (cancelado) |
| 2026-07-27 | #374 | **Promover um helper privado a método público de agregado é mudança de CONTRATO, não só de visibilidade — e o modo de falha "inalcançável por construção" deixa de sê-lo.** GAP-DP moveu a extensão *privada* `Board.withCards` do `SimulationEngine` para um método público do agregado. Comportamento 100% preservado para o único chamador — e ainda assim uma brecha: a versão privada nunca podia receber card órfão (todo card vinha de `board.steps` ou do `firstStep` do mesmo board), a pública admite qualquer entrada, e o descarte silencioso do órfão virou contrato. Não é hipotético: `CardSurrogate.stepId` é serializado à parte do aninhamento em `StepSurrogate.cards` e o decode não reconcilia os dois, então um blob divergente perdia o card — e como `JdbcSimulationRepository` re-serializa o agregado inteiro a cada save, isso é **deleção permanente**, mesma família do #366. **Desfecho contra-intuitivo:** o remédio sugerido pelo harness (tipar a falha, `Either` + `StepNotFound`) estava **errado** aqui — falhar o load só troca a *forma* da perda e `migrations.md` manda preservar para estado vivo; a correção foi o leitor achatado do agregado carimbar o step que guarda o card (o aninhamento é a autoridade). Regra: num widening, exija um de três desfechos — falha tipada, preservação, ou nome/KDoc que anuncie — e escolha pelo tipo do dado, não por reflexo. Harness P2. | `.claude/agents/pr-harness.md` (§2.5 — bullet de widening de visibilidade) |
| 2026-07-27 | #374 | **Property test escrito junto com a correção tem de ser executado contra o código SEM a correção — senão a lei pode não provar nada.** A melhoria "property test para o par leitura/escrita" foi aplicada e as 4 leis passaram; rodando-as contra o código **sem** o carimbo, passaram de novo — teste vácuo. O gerador sorteava o `step` de cada card só entre os steps **do próprio board**, e apontar para um step irmão é **relocação**, não perda: a deleção exige um `StepId` que não existe no board. Corrigido incluindo o id órfão no sorteio; agora `never loses a card` falha sem a correção e as outras 3 seguem passando — a prova ficou assimétrica, que é o que a torna prova. Risco concentrado em propriedades de **preservação** (não-perda, round-trip), onde o gerador tende a produzir só o caso benigno. Uma lei vácua é pior que teste ausente: parece cobertura. Colhido por auto-verificação, não por revisor. | `.claude/rules/testing.md` (Known Pitfalls), `.claude/agents/pr-harness.md` (§2.5) |
| 2026-07-27 | #374 | **Corpo de PR desatualizado ou com alegação falsa não é nit — é o insumo do `post-merge-harvester`.** O corpo do #374 (a) documentava só o 1º commit, omitindo `allCards()`, uma **segunda API pública nova do agregado**, e seu call site; (b) dizia "5 casos" com 8 no arquivo; e (c) afirmava uma regra de Kotlin **invertida** ("membro vence extensão, logo manter a extensão faria o call site resolver para ela" — se o membro vence, resolve para o membro), além de vácua porque os nomes divergiam. Importa porque o corpo é de onde este projeto **destila lição durável** pós-merge: uma "regra" falsa ali vira emenda de skill. As três causas são a mesma — o corpo foi escrito no 1º commit e **não acompanhou** os commits que nasceram da revisão. **Quarta ocorrência** do padrão "mecanismo afirmado por leitura, refutado por verificação" (#326, #332, #369), e nas quatro quem mediu foi o revisor, não o autor; aqui o mecanismo correto **já estava escrito** em `.claude/rules/testing.md:35`. Harness P3, colhido pelo `post-merge-harvester` por estar ausente deste PR. | `.claude/agents/pr-harness.md` (§2.5 — bullet do corpo do PR) |
| 2026-07-27 | #375 | **O snippet do Board Protocol — a primeira coisa que toda sessão roda — estava quebrado em dois eixos ao mesmo tempo, e os dois falham para "Doing vazio".** `.claude/rules/workflow.md` mandava `gh project item-list … --format json` **sem `--limit`** (o `gh` trunca em **30**; o board passou de 130) e filtrar por **`.status.name`**, que **aborta** o `jq` (`Cannot index string with string "name"` — `.status` é string). Qualquer um dos dois devolve vazio, que o protocolo lê como "Doing vazio" → puxa item novo com outro em andamento, **quebrando exatamente o WIP=1 que o guard existe para proteger**. Descoberto porque o `post-merge-harvester` (que usava `--limit 100`) reportou "card GAP-EP não existe" e contagens de coluna erradas — o card existia. Regra: **guard cujo modo de falha é indistinguível do caso benigno tem de ser exercitado, não lido** (mesma família de #288/#367); e todo snippet que pagina API tem de declarar limite folgado. Corrigido nos 3 sítios (`workflow.md`, `/xp-kanban`, `post-merge-harvester`). | `.claude/rules/workflow.md`, `.claude/skills/xp-kanban/SKILL.md`, `.claude/agents/post-merge-harvester.md` |
| 2026-07-27 | #376 | **Um commit pushado depois do squash merge some do PR sem aviso — e o pós-merge o apaga.** O `2d94fc1` (melhoria #2 da revisão do #374) foi pushado minutos após o merge: o PR já estava fechado, então não entrou nem apareceu — medido, `gh pr view 374` lista 4 commits terminando em `c320545` e `headRefOid=c320545`, **congelado no merge**, sem sinal do commit novo. O `post-merge-harvester` teria apagado a branch a seguir, e o commit só sobreviveu porque o mantenedor conferiu a main na mão (virou o #376). Não é azar de timing: com p50 open→merge de **0,5 h** e ~70 PRs/semana, pushar dentro de uma janela já mergeada é o regime normal. **O guard tem de ser a comparação com `headRefOid`, e as três alternativas óbvias foram medidas e falham:** `git branch -d` **não protege** — apaga branch com commit não-mergeado saindo **0**, com aviso `…merged to 'refs/remotes/origin/<b>', but not yet merged to HEAD` **textualmente idêntico** ao do caso benigno (mais uma da família "modo de falha indistinguível do benigno", #288/#367/#375); `git diff main..<branch>` alarma sempre que a main anda (= sempre, nesta cadência); e `git cherry` alarma em toda branch multi-commit squashada (o #376 tem 3 commits). A perda mora no `push --delete`, não no `-d` local: antes dele o commit ainda é alcançável por `remotes/origin/<branch>`. Complementa o guard já existente da §0 — lá "não apague **antes** do merge" (fecha o PR sem merge), aqui "não apague **além** do merge". Colhido por auto-verificação: o #376 não teve revisor (Codex/harness ausentes, Copilot sem quota). | `.claude/agents/post-merge-harvester.md` (§1.2 — guard + por que não simplificar), ponteiros em `.claude/rules/workflow.md` e `.claude/skills/xp-kanban/SKILL.md` |
| 2026-07-27 | #377 | **Escrever um guard reintroduziu, duas vezes seguidas, o modo de falha que o guard existe para matar.** O #377 acrescentou ao `post-merge-harvester` a checagem "a branch andou além do merge?". (1) Ao aplicá-la, o agente mediu que `git rev-parse --verify -q` em ref inexistente sai **1**, o que sob `set -e` abortaria o script de quem copiasse o snippet — falha silenciosa, família do `mktemp` BSD/GNU do #368. (2) O Codex então pegou **dois P1** no que restou: a cadeia terminava em `… || echo "PARE"`, e **`echo` sai 0**, então nem `set -e` interrompia — o passo de delete rodava logo abaixo, com o alerta impresso acima (medido); e o `push origin --delete` incondicional reabria um **TOCTOU** — entre a conferência e o push cabe um push de terceiro, que seria apagado. Remédio medido nos dois sentidos: `if … then exit 1` de verdade, e delete amarrado por `--force-with-lease=<ref>:$TIP` (com push concorrente: `! [rejected] … (stale info)`, exit 1, branch preservada; sem: apaga o commit alheio com exit 0). Regra: **guard é código de segurança — escreva o caminho de falha, execute os três caminhos (benigno / nada-a-fazer / hostil) e confirme que o hostil PARA**; "imprime aviso" não é parar. | `.claude/agents/post-merge-harvester.md` (§1.2 e §1.3) |
| 2026-07-27 | #378 | **O guard do #377 falhou no primeiro uso real — `git fetch origin "<branch>"` não poda ref de rastreamento.** Ao apagar a branch do próprio #377 (o primeiro exercício do guard em produção), o delete amarrado levou `! [rejected] … (stale info)`. Causa medida: o GitHub auto-apaga a branch no merge, e um fetch **por refspec** falha com `couldn't find remote ref` — engolido pelo `|| true` — deixando `origin/<branch>` **stale**; `$TIP` fica não-vazio no caminho mais comum e o passo 3 tenta apagar o que já não existe. `git fetch --prune origin` esvazia o ref e o passo 3 pula, como projetado. Não houve perda (a branch já não existia), mas o guard falhava ruidosamente **no caminho normal** — e guard barulhento no caso benigno é o que treina o operador a ignorá-lo. Regra: **exercite o guard no caminho MAIS COMUM, não só nos casos que ele existe para pegar**; os três cenários que testei no #377 (tip igual / remota apagada / tip divergente) usaram remota apagada **local**, que não reproduz o auto-delete do servidor. | `.claude/agents/post-merge-harvester.md` (§1.2) |
| 2026-07-27 | #378 | **Documentei o passo como cerimônia quando o mantenedor o mantém como REDE DE SEGURANÇA — e a redação errada quase virou a prescrição errada.** O `push --delete` da remota estava escrito como "a remota costuma ser auto-deletada no merge — tolere 'remote ref does not exist'", o que o trata como ruído a engolir. O mantenedor esclareceu o propósito: é a **dupla checagem** contra branch mergeada que ficou para trás, para o GitHub não acumular lixo. Sob esse propósito o passo muda de forma: quando a remota **ainda existe**, apagá-la é o serviço prestado e o mantenedor **quer ser avisado** (o auto-delete falhou); quando não existe, seguir em silêncio. Verificado que o fix de `--prune` preserva a rede (prune só remove ref de branch inexistente; com a branch viva o delete executa — medido) e que o repo está limpo hoje (`delete_branch_on_merge=true`, 4 branches remotas, todas com PR aberto). Regra: **antes de classificar um passo como ruído tolerável, confirme o propósito com quem o mantém** — "tolere o erro" e "este erro é o sinal" levam a códigos opostos. | `.claude/agents/post-merge-harvester.md` (§1.3 — propósito + varredura de branch órfã) |
| 2026-07-27 | #378 | **`\|\| true` é certo quando a falha é resposta legítima e errado quando é ignorância — e no mesmo guard convivem os dois casos.** Ao trocar o fetch por refspec por `git fetch --prune origin`, mantive o `2>/dev/null \|\| true` que existia para engolir o `couldn't find remote ref` do formato antigo. Medido: com a branch alvo apagada o fetch da origin **inteira** sai **0** — o motivo do `\|\| true` tinha deixado de existir —, enquanto remote inacessível sai **128**. Engolindo esse 128, `$TIP` passa a vir do cache: stale (traz de volta o `(stale info)`) ou, sem ref local, **vazio** — e então o guard não confere nada, o passo 3 pula e o harvester **reporta limpeza feita sem ter olhado a remota**, o falso-verde da família GAP-CC. Contraste com a linha seguinte, onde `rev-parse -q \|\| true` **está certo**: ali o exit 1 significa "o ref não existe", que é uma resposta legítima. Regra: pergunte se o comando que falhou **respondeu** ou **não soube** — engolir resposta é ok, engolir ignorância fabrica verde. Terceira rodada seguida em que o defeito deste guard foi um erro engolido. Codex P2. | `.claude/agents/post-merge-harvester.md` (§1.2 — callout `\|\| true`) |
| 2026-07-27 | #379 | **Um fix pode quebrar o passo seguinte por um acoplamento que ninguém escreveu — aqui, `--prune` desarmou o `git branch -d`.** O `--prune` do #378 removeu `origin/<branch>`, que era a via pela qual o `-d` considerava a branch "merged" (em **squash merge** os commits não são ancestrais da main, então a via por ancestralidade nunca valeu). Resultado: o passo 3 do harvester passou a recusar **sempre** com `not fully merged` — regressão introduzida pelo fix do passo 2, invisível na revisão porque os dois passos parecem independentes. Achado ao executar o guard no pós-merge do próprio #378 (**quarta** rodada seguida em que o defeito deste guard só apareceu na execução, nunca na leitura). Correção: comparar **conteúdo** com a main (`git diff --quiet main <branch>` — 0 idêntico, 1 diferente, medido) e então `-D`; é garantia mais forte que o `-d` jamais deu, porque pega commit local não-mergeado em vez de confiar em ancestralidade de SHA. Regra: **ao mudar um passo, execute o seguinte** — acoplamento por estado compartilhado (aqui, um ref de rastreamento) não aparece no diff. | `.claude/agents/post-merge-harvester.md` (§1.3) |
| 2026-07-27 | #379 | **Escrevi, no mesmo arquivo, a armadilha que ele documenta 30 linhas acima.** O §1.2 lista `git diff main..<branch>` como inutilizável para "a branch andou?" porque **falso-alarma sempre que a main anda** (p50 open→merge de 0,5 h aqui). A correção do passo 3 usou `git diff --quiet main <branch>` — a mesma comparação — e, com um commit alheio na main, sai 1 e **aborta a limpeza inteira** (branch **e** board) num caso benigno. Passou nos meus testes porque a main era, naquele instante, exatamente o squash da branch: **o teste confirmou o timing, não o mecanismo**. Correção: comparar o tip local com `$PR_HEAD` (que o passo 2 já buscou e que **congela no merge**), nunca com a main — medido com a main andando: `diff --quiet` → exit 1, `LOCAL == $PR_HEAD` → segue; e com commit local extra → para, branch preservada. Regra: **ao escrever um guard, releia as armadilhas que o próprio arquivo já lista** — a que me pegou estava escrita por mim, no mesmo documento. Codex P1. | `.claude/agents/post-merge-harvester.md` (§1.3 + callout "nunca compare com a main") |
| 2026-07-28 | #370 | **Bump de plugin de build muda *semântica de API*, não só versão — e o dependabot não lê o comentário que a sua config depende.** O `org.graalvm.buildtools.native` 1.1.5→1.1.6 trouxe "Fix custom Gradle binary classpath wiring" (native-build-tools#939): binários customizados passaram a **nascer com o runtimeClasspath principal**. A config do `kanban-vision-migrate` usava `classpath(...)`, que **acrescenta** a uma `ConfigurableFileCollection` — antes era a coleção vazia, depois virou "app inteiro + o meu". O binário de migração engoliu Ktor/Netty/Koin/Lettuce (43.898 → 72.588 compilation units; imagem 282 → 331 MB; image heap do Job 29 → 55 MB, fora do envelope 256Mi da ADR-0036 e com `--gc=epsilon`, que nunca libera) — e o que se perdeu foi exatamente a invariante que o comentário do build declarava proteger ("sem Netty/Ktor no classpath o problema não existe"). **Nenhum gate reprovou**: testes, PITest, SCA e o smoke passaram, porque o único sintoma era um número *impresso* no Smoke Test Report. Duas regras: (1) **um bump de plugin/toolchain merece o mesmo diff de comportamento que um bump de lib com CVE** — leia o release note à procura de "fix … wiring/default/classpath", não só da versão; (2) **invariante que só existe como comentário não existe** — a que estava escrita em prosa há meses caiu no primeiro bump que a contrariou. Diagnóstico sem gastar build nativo: comparar `Top 10 origins of code area` do log do `native-image` entre os dois builds (ele nomeia o jar que entrou) e inspecionar `binaries.getByName("migration").classpath.files`. | `.claude/skills/graalvm/SKILL.md` (§7, 2 pitfalls) + guard `:http_api:verifyMigrationClasspath` no `check` (reprova se `netty\|ktor\|koin\|lettuce` vazar) + `classpath.setFrom(...)` em `http_api/build.gradle.kts` |
| 2026-07-28 | #381 | **Uma sugestão de revisão pode contradizer uma regra MEDIDA do próprio repo — e a resposta certa não é obedecer nem recusar, é medir.** O harness pediu, em `applyUnblock`, trocar `.onRight { }` por `unblock().getOrNull()?.let { }` para o `Movement` depender do sucesso (a *forma* está certa: se guard e regra divergirem, grava-se `Movement(UNBLOCKED)` sem mudança de estado → evento `CardUnblocked` → métrica mente). Só que isso é exatamente o **ramo morto** proibido por `.claude/rules/kotlin-quality.md` — regra nascida no **#350, no mesmo `SimulationEngine`, por sugestão do próprio harness**. Medido pelo autor no PR e **reproduzido de forma independente na colheita**, com `applyBlock` como controle intocado: `.onRight` = BRANCH 0 missed/4, INSTRUCTION 0/50; `getOrNull()?.let` = BRANCH **1 missed**/5, INSTRUCTION **2 missed**/56. O ramo morto é **inerente a condicionar ao `Either`** (`?.let`, `?: return null`, `fold`, `map`+unwrap — todos criam aresta cuja falsa é inalcançável sob o pré-guard), não do açúcar escolhido. **Terceira saída, que é a certa: dissolver, não deslocar** — dentro de uma HOF (`mutateCardAt`, **GAP-EA**, já no Todo) o ramo vira alcançável e testável com um `op` que devolve `Left` de verdade. **Primeira INVERSÃO do log:** as linhas #326/#332/#369/#374 registram "mecanismo afirmado por leitura, refutado por execução — e nas quatro quem mediu foi o revisor, não o autor"; aqui foi o contrário. A regra durável é portanto **simétrica: decide quem mede, não o papel** — e um pitfall medido em `rules/` vence o casamento-por-forma do checklist do rubric. | `.claude/rules/kotlin-quality.md` (contra-exemplo medido + ponteiro p/ GAP-EA), `.claude/agents/pr-harness.md` (§2.5 — cross-check antes de recomendar "condicione ao `Either`"), `.claude/skills/pr-review/SKILL.md` (nova seção "Como responder a um achado") |
| 2026-07-28 | #381 | **A enumeração dentro de um achado é PISO, não teto — varra a classe do problema, não as instâncias citadas.** O P3 de duplicação apontava `simulationFrom` copiado em **duas** classes; um grep mostrou **três** (`…GuardBehaviorTest`, `…MetricsBehaviorTest` e a nova `…MoveGuardBehaviorTest`), e duas já haviam **divergido** (uma com `agingDays`, outra sem) — a prova de que a falha descrita pelo achado já estava acontecendo. Corrigir só as citadas seria meio-consertar: a divergência silenciosa seguiria possível entre as remanescentes. Autor removeu as três e promoveu para `SimulationEngineTestSupport.kt` (−54/+26, test-only). Mesma família do "enumerar vaza" (#367/#368), agora no lado de **quem recebe** o achado. Corolário de teste: dividir classe por `LargeClass` não autoriza copiar o fixture — ele vai para o `*TestSupport.kt` do pacote, com comentário dizendo por que mora lá. | `.claude/skills/pr-review/SKILL.md` (bullet "enumeração é piso"), `.claude/rules/testing.md` (Known Pitfalls — fixture compartilhado) |
| 2026-07-28 | #381 #382 | **Inline órfão prova que a POSTAGEM começou, não que a revisão terminou — e tratá-lo como parecer transforma um desconhecido em aprovado.** A verificação da skill (nascida do #365) filtra só `<!-- pr-harness-report:<sha> -->`; no #381 ela devolve vazio, embora dois P3 inline **ancorados** (`<!-- pr-harness:d9641430… -->`) existam, tenham sido respondidos e resolvidos. A primeira redação desta lição concluiu daí um quarto estado benigno — "revisado num head anterior; se o delta for test-only, registre e siga" — e **estava errada**: o harness publica cada achado conforme o encontra e só ao final emite o veredito, então um run que morre no meio (crash, `Login expired`, timeout, saldo) deixa exatamente esse rastro, com **quantos achados faltaram indeterminado por construção**. Foi o que aconteceu no próprio #381 — o dispatch manual morreu com `Login expired`, não houve report em **head nenhum**, e ele mergeou sem revisão completa de head algum. Fail-open da mesma família do `grep -v` engolindo `exit≠0` no guard de isenção. Correção: o quarto estado é **diagnóstico** ("um run começou e não terminou" ⇒ **rerode**), nunca licença; a regra do delta (`git diff --name-only X Y` → `review-exemption.sh --paths -`) só vale a partir de um **report ancorado**. Regra durável: **ao catalogar um estado novo de um guard, pergunte que evidência ele realmente carrega** — "houve saída parcial" e "houve conclusão" produzem o mesmo rastro visível, e só o segundo autoriza. Codex P1 no #382, sobre a emenda que este mesmo ciclo tinha acabado de escrever. | `.claude/skills/pr-review/SKILL.md` (passo 1b + o quarto estado, corrigido) |
| 2026-07-28 | #383 | **Nem todo campo degradado é equivalente — degradar a CHAVE DE ESCRITA troca um 500 barulhento por corrupção silenciosa.** A regra de decode tolerante (`migrations.md`, nascida no #355) prescreve "coaja a sentinel" **por campo**, e foi aplicada à risca — inclusive ao `id` de topo do blob. Só que `JdbcSimulationRepository.save` faz **upsert por `simulation.id`**: a simulação carregada por `findById(idReal)` seria regravada no `runDay` seguinte numa **linha nova** sob `"(unknown)"`, deixando a original órfã e os snapshots seguindo o id errado — pior que o erro que a tolerância evita, porque é silencioso e permanente. A distinção que faltava: para todos os outros campos o blob é a única fonte, mas a **identidade tem fonte autoritativa fora dele** — a linha relacional que a query já usou. Fix: o serializer segue sem lançar (é o ponto do gap) e `rowToSimulation` repõe a autoridade (`decode(json).copy(id = SimulationId(row[…]))`). Pergunta durável a fazer de cada campo degradado: **alguém escreve POR ele?** E o teste tem de **regravar** o agregado e contar as linhas — carregar não revela o fork. Codex P1. | `.claude/rules/migrations.md` (nova seção "Coagir a sentinel NÃO basta"), `.claude/agents/pr-harness.md` (§2.5) |
| 2026-07-28 | #384 #385 | **Corrigir UM caso de uma classe não fecha a classe — enumere todos os campos que o critério alcança.** A emenda escrita a partir do P1 do #383 dizia, em tempo presente, que "para todos os outros campos o blob é a única fonte", e o fix reconciliava só o `simulation.id`. Mas `organization.id` satisfaz o **mesmo critério** ("alguém escreve POR ele?"): vem da mesma linha do join, é regravado pelo `save`, é coluna com **FK** (`simulations.organization_id REFERENCES organizations(id)`) **e** é a chave de tenancy dos 5 use cases (`ensure(simulation.organization.id == callerOrganizationId) { Forbidden }`). Degradá-la dava FK violation no save e **403 para o dono legítimo** — o registro "tolerado" ilegível na prática, o oposto do objetivo do GAP-DV. Codex P2 no #384, ou seja: **o defeito estava na própria lição**, escrita com a generalização não verificada ("todos os outros") em vez do critério aplicado exaustivamente. É a mesma família da linha do #381 sobre enumeração-é-piso, agora do lado de **quem escreve a regra**, não de quem recebe o achado: ao formular um critério, rode-o sobre o código inteiro antes de afirmar o complemento ("todos os outros são X"). Código corrigido em #385, com o teste falhando por **violação de constraint** — sem reconciliação o re-save nem chega a uma asserção. | `.claude/rules/migrations.md` (callout "enumere TODOS os campos" na seção nova) |
| 2026-07-28 | #383 | **Um caminho de REPARO que você inventa tem de satisfazer os invariantes CROSS-FIELD do agregado que ele vai construir — coerção campo-a-campo não os enxerga.** O decode completava worker sem abilities com o fallback **global** (`DEVELOPER`), mas `Step.init` exige `workers.all { hasAbility(requiredAbility) }`: num step de `TESTER`/`DEPLOYER`/`PRODUCT_MANAGER` o `init` continuava lançando e o agregado seguia não-carregável **exatamente no caso que a tolerância cobria**. O sinal de revisão é específico: o autor **acertou** esse raciocínio para o enum ilegível (fallback aplicado nos **dois** lados = renomeio consistente, que preserva o invariante) e **não o estendeu ao caminho de reparo que ele mesmo criou** — verifica-se o caminho novo contra o **construtor-alvo**, não contra a regra que o inspirou. Fix: `StepSurrogate.toDomain` decodifica a ability primeiro e a repassa ao decode de cada worker. Três condições que a reparação contextual tem de respeitar: **ordem** entre reparos (a ability do step entra antes da regra `TESTER → DEPLOYER` do `Worker.init`), **só acrescenta, nunca remove** (podar o worker incompatível resolveria o `init` mas é descarte, permanente no próximo save), e **não é só o caso vazio**. Corolário para escolher a constante: o fallback **não pode participar de nenhum outro cross-field** (daí `DEVELOPER`, não `TESTER`, que exigiria `DEPLOYER`). Codex P1. | `.claude/rules/migrations.md` (mesma seção + as 3 condições), `.claude/agents/pr-harness.md` (§2.5) |
| 2026-07-28 | #383 | **Um teste que CORROMPE um fixture tem de assertar que a corrupção aconteceu — senão ele testa o caminho feliz, em verde.** Mesmo vácuo do property test do #374, um nível antes: o teste de tolerância zerava o `id` no blob com `replaceFirst` e afirmava "ainda carrega". A armadilha concreta: `state_json` é **JSONB**, e o Postgres guarda o valor **parseado** — o que volta é re-renderizado (espaço após `:`; ordem e chaves duplicadas também não se preservam, conforme a doc do `jsonb`), **não** os bytes que `SimulationSerializer.encode` gravou. Casar o literal do encoder contra um blob **lido do banco** falha em silêncio, e o único motivo de eu ter percebido foi a asserção anti-vácuo (`check(blanked != blob)`) falhar primeiro — sem ela o teste passaria sem corromper nada, provando zero. Generaliza para qualquer store que **normaliza** o que guarda (JSONB, formatter, ORM que re-serializa): nunca assuma que uma string round-tripped é byte-idêntica à escrita. Colhido por auto-verificação. | `.claude/rules/testing.md` (Known Pitfalls) |
| 2026-07-28 | #385 | **Achado de revisão se corrige NO PR que o gerou; só a LIÇÃO é que se avalia agora-ou-depois.** Diretriz explícita do mantenedor: *"para estas demandas encontradas, corrigir já nesse PR, não há necessidade de deixar para depois, o review serve para isso, somente lições aprendidas devem ser avaliadas se resolvemos agora ou depois."* O parecer do #385 tinha feito o oposto: classificou o P2 como "pré-existente na main, fora do diff, NÃO é pedido de mudança" e o encaminhou como "candidato a card `[N]`" — e estava errado, porque era **outra instância da mesma classe** que o PR já consertava (tolerância do decode nos mesmos serializers). Adiar teria aberto uma **terceira** rodada da mesma classe (#383 → #384 → #385). Fica uma disposição de três destinos, e só um é adiável: **achado ⇒ agora**; **melhoria (§3.5) ⇒ candidato, mantenedor decide**; **lição ⇒ `post-merge-harvester`**. Dois limites estruturais, não desculpa de escopo: se o fix é `[E]`/outro gap, vira card **antes** do PR; e um PR doc/processo **não pode** absorver código (o guard anti-loop classifica por `*/src/main/**`) — foi o caso do #384, e é exatamente aí que o card tem de vir antes. | `.claude/skills/pr-review/SKILL.md` (nova subseção "Disposição" + tabela), `.claude/agents/pr-harness.md` (callout: `Correção:` é para ESTE PR, não "candidato a card") |
| 2026-07-28 | #385 | **Nunca abrir PR sem card priorizado — e o card vem ANTES, não depois.** O #385 nasceu de um P2 do Codex no #384 e foi aberto por conta própria, sem card; o GAP-EU foi criado depois, a pedido do mantenedor, para regularizar. Retro-cardar conserta o registro, **não** a decisão de ordenação que foi pulada: quebra o WIP=1, tira o board de fonte única (ADR-0023) e infla o lead time (o relógio do card começa depois do trabalho). A regra é o **complemento** da linha acima — as duas metades de uma coisa só: quando o achado **cabe** no PR que o gerou, corrija lá (e não há card novo nenhum); quando **não cabe** (fix `[E]`/outro gap, ou PR de doc que não pode absorver código), é **pergunta → card → priorização → branch**. O caso "não cabe" é o que produz PR novo, e é justamente onde a tentação de pular o card aparece. Colhido da cobrança do mantenedor, não de revisor. | `docs/politicas-explicitas.md` (§4 — corolário da pull policy), `.claude/rules/workflow.md` (Session Start) |
| 2026-07-28 | #385 | **Uma emenda de regra tem de prescrever o PROCEDIMENTO verificável, não a CONCLUSÃO — a conclusão envelhece em silêncio.** Duas redações seguidas da mesma regra em `migrations.md` falharam do mesmo jeito: a do #383 afirmou "para todos os outros campos o blob é a única fonte" e a do #385 fechou em "as **DUAS** identidades" — enquanto `save` regrava **cinco** valores (`id`, `organization_id`, `wip_limit`, `team_size`, `seed_value`), sendo `wip_limit`/`team_size` **também** degradados no decode (`coerceAtLeast(1)`). Conclusão não se verifica; procedimento se roda. A regra passa a pedir a **lista classificada**: enumere as colunas que o `save` escreve a partir do agregado decodificado, classifique cada uma como **autoridade externa** (reconciliar) ou **projeção** (não reconciliar), e **registre a condição que moveria uma projeção para a primeira lista**. Distinção que a conclusão escondia: **ser degradado não implica reconciliar** — o critério é a autoridade. Harness P3; o autor adotou a forma no comentário do código e a lição fecha o par. | `.claude/rules/migrations.md` (procedimento de 3 passos na seção "Coagir a sentinel NÃO basta") |
| 2026-07-28 | #385 | **Ao corrigir um achado, varra a SUBÁRVORE alcançável — a unidade de enumeração é o construtor, não o campo citado.** Segunda ocorrência do "enumeração é piso" (#381), agora com **payoff medido** e do lado de quem recebe o achado: o parecer listava os ids e os quatro números de `FlowMetrics` como pontos que lançavam no decode do `history`; enumerar a subárvore inteira revelou `SimulationDay` (`require(value >= 1)`, verificado em `domain-simulation/.../SimulationDay.kt`), que ninguém citara — um `day: 0` legado lançava igual a um id vazio, produzindo o mesmo 500 em `findById` e na página inteira de `findAll`. Daí o helper `decodeDay`. Duas armadilhas específicas do "por campo": **invariante numérico conta tanto quanto string em branco**, e **o mesmo tipo decodificado em dois lugares precisa dos dois consertos** (`DailySnapshot` é decodificado pelo serializer do estado vivo **e** pelo do snapshot — corrigir um deixa o outro no caminho de 500). | `.claude/rules/migrations.md` (bullet da subárvore alcançável), `.claude/skills/pr-review/SKILL.md` (bullet "enumeração é piso" — precedente com payoff) |
| 2026-07-28 | #385 | **Uma afirmação refutada sobrevive em CÓPIAS — o PR que a refuta tem de grepar a redação antiga.** O #385 existia para derrubar a afirmação do #383 (*"a identidade de topo é diferente dos demais campos"*); o comentário do repositório foi atualizado para `#383 P1 · #384 P2`, mas o **KDoc do teste-guarda** seguiu com o texto refutado — vivo dentro do arquivo cujo segundo teste o contradiz. Não é cosmético: doc que afirma um fechamento inexistente é a mesma classe de defeito que o PR estava consertando, um nível abaixo do corpo do PR (bullet vizinho, #374). Procedimento: 3–5 palavras características da redação antiga + `grep -rn`; toda cópia entra no mesmo PR. Harness P3. | `.claude/agents/pr-harness.md` (§2.5 — bullet "PR que refuta uma afirmação") |
| 2026-07-28 | #385 | **A nota de mutação de um módulo pode cair vários pontos por um TESTE que parou de rodar — não pelo diff.** `sql_persistence` foi de **75% → 72%** (gate 65) com um diff de 4 arquivos. Medido base-vs-head mutante a mutante, em duas rodadas (535/741 nas duas, logo não é flakiness): o per-arquivo dos arquivos tocados é **idêntico**, exceto `LegacyDecode.kt` **15 → 16 killed** (o diff *acrescentou* um mutante morto); os 22 mutantes perdidos estão em `DatabaseFactory.kt` (15) e `SimulationStateSerializer.kt` (7), **não tocados**; e `TIMED_OUT` subiu **19 → 22** porque o teste lento `…flyway migrates from the custom path` caiu de **20 kills para 2**. Ou seja: mais uma classe de Embedded PostgreSQL no módulo (cada uma sobe banco novo + Flyway) estoura o orçamento de tempo do PITest, e **o teste que matava aqueles mutantes deixa de terminar**. Ordem de diagnóstico: per-arquivo base-vs-head e, se a perda está em arquivo não tocado, ler `TIMED_OUT`/kills-por-teste antes de escrever "regressão de cobertura". ⚠️ **Das duas saídas, uma é ruim e a outra é decisão do mantenedor** (correção do Codex P2 no #386 — a primeira redação dizia que as duas estavam "barradas por política", o que é falso): fundir classes de teste para agradar a ferramenta está fora, mas **subir o `timeoutConstInMillis` não é bypass** — a regra proíbe editar `build.gradle.kts` *para contornar violação*, e deixar o teste matador terminar é o oposto disso (o bloco nem configura timeout hoje: roda no default do PITest). O que impede não é imutabilidade, é a regra card-first — é trade-off de build (timeout maior deixa toda rodada mais lenta e pode mascarar mutante travado), logo decisão do mantenedor. Acima do gate: **meça, registre, proponha; nunca ajuste por conta própria**. Colhido por auto-verificação do autor. | `.claude/rules/kotlin-quality.md` (extensão do base-vs-head — tabela de evidências + ordem de diagnóstico) |
| 2026-07-28 | #383 | **`base-vs-head` também vale para SOBREVIVENTES do PITest — e absolve tanto quanto acusa.** A regra já estava escrita em `kotlin-quality.md` para *partials* de JaCoCo (#366). Medindo o baseline de `surrogateServiceClass` na **main** (1 sobrevivente) ficou claro que a minha versão tinha **2**: o extra era um `?:` redundante que eu havia introduzido, invisível enquanto o módulo seguia acima do gate (71% ≥ 65%). Não é lição nova — é a mesma regra numa métrica que ela não nomeava; registrada como extensão de uma linha, não como entrada própria. | `.claude/rules/kotlin-quality.md` (extensão da regra base-vs-head) |
| 2026-07-29 | #388 | **Usar "nenhum PR sem card" para ESTREITAR o escopo de um card é virar a regra contra o fluxo que ela protege.** A regra governa *qual* problema se pode pegar, não *quanto* dele se entrega — e o card nomeia uma **classe de problema**, cujos sítios citados são exemplo, não inventário. No planejamento do GAP-DQ a varredura da classe achou **dois** sítios de `board.steps` cru na produção: o do card (`SimulationDtos.kt:349`) e um adjacente (`SimulationEngine.applyAdd`, reimplementando `minByOrNull { it.position }` — o critério que `Board.stepsInExecutionOrder()` já encapsula desde o #376). A sessão **propôs adiar o segundo** invocando esta mesma regra (e o "Standalone" do card), e o **mantenedor reverteu**: *"por que não puxar isso nesse card?"*. Incluir custou **1 linha de produção + 3 testes** e deixou a produção com **zero** `board.steps` cru (verificado por `rg` no merge: sobram só um comentário e a chamada legítima ao método do agregado). Adiar teria gasto card + priorização + branch + CI + revisão pela mesma linha — o lead time que a regra existe para **reduzir**. "Standalone" no card significa independente de **outros cards**, não licença para meio-conserto. É a "enumeração é piso, não teto" (#381) aplicada ao **escopo do card**, não ao de um achado de revisão; o limite que impede virar licença para alargar é *mesma classe de problema* — outro defeito continua sendo pergunta → card. **Aplicada em modo de exceção por decisão explícita do mantenedor** (contador em 2/10), não pela exceção de "defeito ativo/guard furado" da regra de cadência. | `docs/politicas-explicitas.md` §4 (contra-corolário + precedente), `.claude/rules/workflow.md` (callout no Session Start), `.claude/skills/evolutionary-change/SKILL.md` §7 (anti-padrão "Gap slicing" + tabela dentro/fora) |
| 2026-07-29 | #388 | **Quando a produção passa a chamar um helper, o teste que a valida precisa MANTER o próprio cálculo — senão a lei degenera em `f(x) == f(x)`.** Terceiro membro da família de vácuo já registrada em `testing.md` (gerador que não alcança o defeito; corrupção que não corrompe) e o mais fácil de introduzir sem querer: o refactor que extrai `X.foo()` varre os call sites, e o teste cujo **oráculo era** o cálculo manual parece só mais um deles. No #388 o `DtoMappingPropertyTest` foi **deixado de propósito** com a soma escrita à mão, enquanto a produção passou a chamar `Simulation.itemCount()`. O outro lado da mesma moeda: manter um segundo cálculo do mesmo valor **de propósito** só é legítimo com uma **lei** que amarre os dois — `itemCount()` conta direto em vez de `allCards().size` porque o `allCards()` carimba `copy(step = step.id)` em N cards só para contar, e a igualdade virou property provada **não-vácua por sabotagem** (`+ 1` → vermelho). Otimização sem lei que a prenda à implementação de referência é divergência esperando o primeiro dia silencioso. | `.claude/rules/testing.md` (bullet novo, ao lado dos dois de vácuo) |
| 2026-07-29 | #388 | **O limite do `LargeClass` é do CORPO como o Detekt conta, não do arquivo — e a regra estava escrita como se fosse do arquivo.** Duas linhas normativas (`.claude/rules/kotlin-quality.md` e `.claude/rules/testing.md`) diziam "200 lines" sem qualificar. Medido no #388: `BoardStepCardExecutionBehaviorTest.kt` foi a **240 linhas de arquivo** e passou com folga — ~**174** linhas efetivas de corpo (sem brancos nem comentários, contagem própria) contra o limite 200, num repo que comenta teste pesadamente. Erra nos dois sentidos: leva a **partir classe** que não precisa (custo real — a linha vizinha lembra que partir tenta duplicar fixture, #381) e a registrar um build verde como "passou por sorte". A saída é a mesma dos outros pitfalls medidos deste log: **confirme o gate rodando-o**, não estimando com `wc -l` — mas rodando a task da **source set certa** (`detektMain` para produção, `detektTest` para teste, `detekt` para ambos), sempre com `--rerun-tasks`. A primeira redação desta emenda prescrevia `detektTest` para uma regra que vale para todo `*.kt`: contra uma classe de PRODUÇÃO perto do limite, ela passa **sem examinar a classe** — falso verde da mesma família que esta linha existe para matar (Codex P2 no #389, sobre a própria emenda). | `.claude/rules/kotlin-quality.md` + `.claude/rules/testing.md` (as duas linhas corrigidas) |
| 2026-07-29 | #387 | **Limite conhecido de um guard vira dívida no instante em que vira código — e "eu já sabia" não é mitigação.** Escrevi no plano do #387, com todas as letras, que a fitness function era **driblável por renome** (`repo.findById` escapava da literal casada) e entreguei assim, com o limite documentado no comentário. Codex abriu P2 sobre exatamente isso. Um guard de **segurança** que se contorna renomeando um campo não é guard; documentar o furo só transfere ao revisor a parte barata. **O corolário é o que dói: limite conhecido quase nunca vem sozinho** — o mesmo P2 trouxe uma segunda metade que eu não tinha visto (implicação agrega em nível de classe: uma classe com uma chamada guardada *e* uma carga direta passava). Declarar o furo percebido dá a sensação falsa de ter cercado o problema quando só se cercou a instância percebida. Custo de fechar, no caso: **uma linha**. | `.claude/skills/pr-review/SKILL.md` (seção "O autor dispõe também dos PRÓPRIOS limites") |
| 2026-07-29 | #387 | **Regra Konsist que casa TEXTO se dribla renomeando: ancore no TIPO, e prefira proibição plana a implicação.** Derive o nome do receptor perguntando ao tipo (`clazz.properties().filter { it.type?.name == "X" }`) em vez de fixar a literal. E implicação ("contém A ⟹ contém B") **agrega em nível de classe** — uma classe com uma chamada guardada e uma carga direta satisfaz as duas substrings e passa; sem substring de escape esse buraco não existe. Dois fatos operacionais que empurram o autor para a forma errada e precisam estar escritos: `assertTrue` sobre lista vazia lança `KoPreconditionFailedException` (daí a tentação da implicação — a saída certa é proibição plana **+ teste de não-vácuo separado**), e o probe negativo é barato porque o Konsist lê fontes via PSI e `:architecture:test` não compila os módulos de produto (o probe nem precisa compilar). Bônus medido: uma regra escrita **junto do refactor** nasce vermelha de propósito, e a transição 🔴→🟢 é a prova que `testing.md` exige, sem arquivo descartável. | `.claude/skills/kotlin-quality-pipeline/SKILL.md` §8 |
| 2026-07-29 | #387 | **Deduplicar DERRUBA o mutation score do módulo sem perder detecção — efeito de denominador, e a defesa é aritmética por arquivo.** `usecases` 63,5%→62,1% (gate 55) ao extrair um guard copiado em 5 use cases: cada call site perdeu os 2 mutantes KILLED do `ensure` (saem do numerador **e** do denominador) e o arquivo novo trouxe 9, sendo 5 killed — o resto é andaime de `suspend`, mesmo perfil do `Timed.kt` pré-existente. O guard consolidado ficou MAIS exercitado (5 testes de use case + 3 diretos). A leitura ingênua inverte o sinal de um refactor bom e desincentiva deduplicação. Contraste com o #385: lá a perda estava em arquivo **não tocado** (teste que parou de completar); aqui é aritmética esperada nos arquivos tocados. | `.claude/rules/kotlin-quality.md` (bloco PITest, terceiro bullet) |
| 2026-07-30 | #386 #390 | **Maquinaria de processo introduzida para reduzir atrito virou a maior fonte de atrito — e nunca funcionou uma vez.** A cadência fila→contador→lote→PR de processo (#386) tinha 9 defeitos que se reforçavam: o contador **se auto-zerava** (o commit de *enfileirar* casava o grep do marco), o marco primário era **inalcançável por construção** (o §3.1 mandava um subject que nunca casa `^docs(process): lote`), a fila **fragmentou** em main (1 linha) e branch órfã (4), o próprio harvester classificava essa branch como **lixo a apagar**, o `settings.json` **não permitia push** no prefixo que a cadência usa, e a instrução de uso morava **dentro do arquivo que só existia na branch** (0 hits de `lessons-queue` na main). Zero lotes emitidos em 3 dias. Removida no #390: a lição passa a ser escrita **no PR que a aprendeu**, que já está em revisão — custo zero de ciclo, zero peças móveis. Regra durável derivada: **antes de tocar em processo, pare e pergunte ao mantenedor**; e desconfie de mecanismo de processo que nunca foi exercitado de ponta a ponta — ele não está "pronto", está **não-testado**. | remoção de `lessons-pending.md` + §3/§3.1 do harvester; regra em `docs/politicas-explicitas.md` |
| 2026-08-03 | #401 | **`KoClassDeclaration.containingFile.*` é evidência de ARQUIVO usada para julgar CLASSE — com N classes num arquivo, basta uma conformar e as outras N−1 passam de carona.** Terceiro eixo de furo em fitness function Konsist, distinto dos dois já registrados (ancore no tipo, #387; implicação agrega por classe, #387). Medido: um `Query` acrescentado ao `GetSimulationQuery.kt` cujo `validate()` devolvia `Either.Right(Unit)` cru **passava nas três regras**. A correção é ler a declaração da própria classe (aqui, o corpo do `validate()`) e casar chamada por nome local — o simples, que também casa o sufixo do FQN, mais o alias de import. Regra durável: **ao escrever guard sobre classe, verifique se cada probe tem o mesmo escopo do sujeito**; `containingFile` nunca tem. | `.claude/skills/kotlin-quality-pipeline/SKILL.md` (§8, guards Konsist) |
| 2026-08-03 | #402 | **Um análogo pré-existente do módulo NÃO é linha de base de correção — pode ser o mesmo ponto cego replicado; e `NO_COVERAGE` em `invokeSuspend`/`throwOnFailure` é teste de resume faltando, não andaime.** O #402 abriu para **baixar** o gate do `usecases` de 55 para 50, justificando que 54 dos 62 mutantes `NO_COVERAGE` eram lowering de coroutine insensível a teste — apoiado exatamente na heurística que o #387 tinha deixado na regra ("compare com um análogo pré-existente"). Um P2 do Codex refutou: os mocks do módulo usam `coEvery { … } returns x`, que resolve na mesma pilha, então a continuation nunca é salva e o resume nunca executa; em produção executa sempre (`dbQuery` faz `withContext(Dispatchers.IO)`). Medido — 7 testes com `coAnswers { delay(1); withContext(Dispatchers.Default) { … } }` levaram o módulo de **166/294 (56,5%) para 215/294 (73,1%)**, com `NO_COVERAGE` de **62 → 8**. O PR fechou **sem tocar no gate**. Duas regras duráveis: (1) "todos os arquivos do módulo têm esse perfil" é evidência de **causa comum**, não de inevitabilidade; (2) antes de propor mexer em `mutationThreshold`, prove que o `NO_COVERAGE` é **inalcançável** — conte por mutator. | `.claude/rules/kotlin-quality.md` (heurística do #387 **removida** + item novo do resume) |

> **Nota de processo — as 3 linhas de #374 acima foram gravadas ANTES do merge**, a pedido do mantenedor,
> fora do fluxo normal (o `post-merge-harvester` colhe depois do merge justamente para não registrar como fato
> o que ainda pode mudar). O desvio cobrou: o Codex reprovou o #375 com um P2 porque a emenda citava
> `Board.redistributeCards`/`allCards` em tempo presente e um grep no SHA revisado não achava nenhum dos dois —
> a regra violava o bullet vizinho, que manda grep-verificar cada identificador citado. Correção adotada: citar
> o **PR de origem** em vez de nomear API como contrato vigente. #374 (`1119718`) e #375 (`afb7af1`) estão na
> main desde 2026-07-27, então as linhas já descrevem estado real — **o registro fica como precedente**: colher
> lição antes do merge exige linguagem que não afirme o que ainda não existe.
