# Lessons Learned — the review-to-skills feedback loop

Append-only log of **durable, generalizable** lessons surfaced by a PR/gate — the ones worth changing
the way we work so the same mistake does not recur. It is the durable half of the loop the pr-harness
rubric §6 and the `/pr-review` skill drive: a review surfaces a lesson → it is **recorded here** and, when
it warrants, **applied** to a skill/rule/rubric (or carded on Board #6). Nothing gets lost between "the
harness noticed it" and "the process improved."

**What belongs here:** a recurring miss, a false-negative from a gate/reviewer, a subtle trap that cost
time, a process gap. One entry per lesson. Keep it terse: what happened, the durable rule, where it was
applied. **What does NOT belong:** feature-specific findings (those live in the ADR / the gap's notes),
one-off nits, or lessons without a concrete "apply it here."

**How the loop runs (automated):** when the author tells the session a PR merged, the
**`post-merge-harvester` agent** (`.claude/agents/post-merge-harvester.md`) does the closure AND harvests
this file's lessons — reading the merged PR's *inline* review comments, distilling the durable ones, and
**applying** them as amendments to the target skill/rule/rubric plus a row here, in a ready `[N]`
process-PR. That is the point: lessons become *applied process for the next cycle*, not a task list. The
`/pr-review` skill (step 5) and the pr-harness rubric §6 feed the same file. Feature-specific findings do
NOT belong here — they live in the ADR / the gap's notes.

**Anti-loop guard (why this doesn't spiral):** the harvester harvests **only after a *real implementation***
— a merged PR that touches production source (`*/src/main/**`). A **process/doc/skill/ADR/test-only PR gets
closure only, no harvest**, so an improvement-PR never spawns another improvement-PR (the loop terminates in
one level). This file itself is process — merging it triggers closure only.

| Date | PR(s) | Lesson (the durable rule) | Applied to |
|---|---|---|---|
| 2026-07-21 | #323 #324 #325 | **Read the raw inline review comments, never trust the harness *summary*.** The pr-harness report can say "APPROVE / low severity" while its own inline comments (and Codex) carry P1s — it happened 3× in one cycle, including a *blocking* supply-chain P1. Verify via `gh api .../pulls/N/comments` + the GraphQL `reviewThreads` (`isResolved`), not the prose summary. | `.claude/skills/pr-review` (hard-rule), `feedback_always_run_pr_review` memory |
| 2026-07-21 | #325 | **CI already runs pr-review — read the PR's posted reports; don't re-dispatch the harness by default.** `pr-review.yml` (GAP-CT) posts the `[claude]` report after CI. Manual `/pr-review` is the exception (CI hasn't run yet / immediate feedback). Redundant *and* risky — see next row. | `.claude/skills/pr-review` (default at top) |
| 2026-07-21 | #325 | **The pr-harness subagent runs Bash in the *same* working directory and can `git checkout`.** A manual dispatch switched the branch and reverted files mid-review (the pushed commit was intact — it reviews the remote SHA). If dispatching manually, re-check `git branch --show-current` afterwards. Preferring CI reports avoids this entirely. | `.claude/skills/pr-review` (manual-dispatch caveat) |
| 2026-07-21 | #325 | **Align a transitive dependency family for a CVE via its BOM, not per-module pins.** A per-module Netty pin missed `netty-resolver-dns`/`netty-codec-dns` (pulled at a CVE'd 4.1.x by Lettuce) → the blocking `supply-chain` gate went red. `platform("io.netty:netty-bom:<v>")` aligns the whole family in one line. | `.claude/rules/stack.md` |
| 2026-07-21 | #325 | **Kotlin trap — `/*` (e.g. inside a `` `glob/**` ``) within a KDoc opens a *nested* block comment** (Kotlin supports nesting) that never closes → "Unclosed comment". Avoid `/*` sequences in doc text (write `pkg.sub`, not `` `pkg/sub/**` ``). *(Correction, #326: this — not explicit type args like `evalsha<List<Long>>`, which are valid Kotlin — was the real cause of that build's error. A first draft of this log misdiagnosed it; Codex caught it. Lesson-about-lessons: verify the mechanism before recording it.)* | `.claude/rules/kotlin-quality.md` (pitfalls) |
| 2026-07-21 | GAP-CZ | **A lesson log is not enough — the loop must be *applied and automated*.** Documenting lessons in a file still relies on someone acting on them. Encoded the loop as the `post-merge-harvester` agent (closure + harvest + *apply* to skills/rules as a ready process-PR), triggered whenever the author says a PR merged. | `.claude/agents/post-merge-harvester.md`, `.claude/rules/workflow.md` (After-PR-Merge) |
| 2026-07-21 | #324 | **Verify a library's internals from its source, not from assumption.** Ktor's `DefaultRateLimiter` turned out to be a *fixed-window* counter, not a token bucket — refuting a reviewer P2 and producing ADR-0042. A five-minute read of the jar source settled a claim two reviewers got wrong. | `feedback_full_rescore_verify_code` memory (reinforced) |
| 2026-07-23 | #329 #331 | **A feature PR's `supply-chain` gate can go red on deps it never touched — a fresh OSV disclosure is repo-wide.** Twice in one session (jackson-3 via Flyway 12/logstash 9; a Netty batch) a k8s-only PR was blocked by a new CVE on an already-pinned transitive family. Don't fold the fix into the feature — it's a separate `fix/` PR (bump the family via its BOM). The aggregate SBOM covers each module's `runtimeClasspath`, so repeat the BOM in **every module** that resolves the family. *(Update: **GAP-DA (#TBD) added `migrationRuntime` to `includeConfigs`**, so the migration binary's classpath is now in the SBOM too — before it, a migration-only dep was a blind spot you had to check by hand via `:mod:dependencies --configuration migrationRuntime`.)* Also: the CI `osv-scanner-action@v2.3.8` **fails on an *unused* ignore** (local 2.4.0 only warns), so a stale `osv-scanner.toml` exception blocks the gate by itself. *(Codex P2 on #332 corrected an earlier draft that wrongly implied the SBOM scans `migrationRuntime` — same "verify the mechanism" trap as the #326 row.)* | `.claude/skills/github-ci-health` (§4 signature + §7 pitfall), `project_osv_scanner_ci_gotchas` memory |
| 2026-07-23 | #330 | **Migrating a static-target Prometheus alert to k8s pod SD changes `up == 0` semantics.** With `kubernetes_sd_configs` role `pod`, a *total* outage removes every `up{job=...}` series, so `up == 0` evaluates an empty vector and never fires. Add an `absent(up{job=...})` companion (critical) for "zero targets"; the `up == 0` rule still covers partial outage. Reviewer (Codex) caught it as P1. | `.claude/skills/opentelemetry` (ServiceDown callout) |
| 2026-07-23 | #330 | **A `subPath` ConfigMap mount freezes the file at pod-start — even `/-/reload` rereads the stale copy.** Kubelet only propagates ConfigMap updates to *directory* mounts (symlink swap). Mount config as a directory (no `subPath`) and point the config flag there (or add a reloader sidecar); otherwise a config change needs `kubectl rollout restart`. Reviewer (Codex) caught it as P2. | `.claude/skills/local-and-production-environment` (deployment.yml callout) |
| 2026-07-23 | #333 #335 | **A copied config diverges from its source under k8s — reused compose configs need per-environment label adaptation.** Container-metric selectors (`{name="kanban-vision-app"}` in the Grafana dashboard; `{name!=""}` in rules) use the *compose* cAdvisor label `name`, absent under the k8s kubelet (which emits `namespace`/`pod`/`container`) → panels/alerts silently see no data. Two reviews (Codex P2×2) caught the dashboard and the Alertmanager `inhibit_rules` key. Adapt every copied config's label selectors to the target environment; track the compose↔k8s duplication (GAP-DB). | `.claude/skills/opentelemetry` (scope note) |
| 2026-07-23 | #335 | **An Alertmanager `inhibit_rule` `equal` key only works if EVERY source/target alert emits those labels — and `sum by (...)`/`histogram_quantile` silently drop labels outside the `by` clause.** A first fix keyed inhibition on `['namespace','pod']`, but `HighHttpErrorRate`/`HighHttpLatencyP95` aggregated by `instance` only → those alerts lost `namespace`/`pod` and a critical on one pod inhibited warnings on every pod. Fix: preserve the key labels in the aggregations (`sum by (namespace, pod, instance)`). Verify each alert's `expr` emits the key labels; aggregated label-less alerts (`sum`/`max`/`absent`) match by "absent-on-both = equal" — a conscious residual. Reviewer (Codex) caught it as P2. | `.claude/skills/opentelemetry` (inhibit_rules callout) |
| 2026-07-23 | #341 | **Reusing a cycle/graph detector across gates requires re-validating what a self-loop/self-ref *means* in that domain.** The new DI-wiring cycle gate copied `ClassCycleTest`'s "drop self-ref" filter (`it != impl`) — correct for *data composition* (`TreeNode(children: List<TreeNode>)`, legitimate) but wrong for *eager DI construction* (self-injection `single { A(get()) }` / `class A(other: A)` → real Koin `StackOverflowError`). The inherited filter blinded the new gate to the simplest cycle it promised to catch (Codex + harness **P2**). Also (harness **P3**): a `associate { it.name to … }` lookup by simple name silently collapses homonyms last-wins — fail loud on collision instead (the reason `ClassCycleTest` uses FQN, GAP-CV). | `.claude/agents/pr-harness.md` (§2.5 checklist), `.claude/skills/circular-dependency-control` (automated-detection note) |
| 2026-07-24 | #345 | **A *cancelled* `pitestAll` run reports `0 mutants`/`0%` for the not-yet-executed modules — a false ❌, not a mutation regression.** `pitestAll` declares all 6 module tasks via `dependsOn` (unordered) with `org.gradle.parallel=true`, so a cancelled in-flight run leaves **whichever** modules hadn't finished at `0 mutants / 0%` — non-deterministic (here it was `usecases`/`http_api`, but they are **not** guaranteed to run "last"/serially, contra a first draft of this note; Codex P2 on #346). The tell is **0 mutants generated** (a real coverage drop keeps the mutants, only SURVIVED rises). Check the run status (`gh run view <ID> --json conclusion` → `cancelled`) and reproduce locally (`:usecases:pitest` → 311 mutations, 63% ≥ 55%) before treating it as a regression. | `.claude/skills/github-ci-health` (§4 signature) |
| 2026-07-23 | #338 | **A foreground command in a CI step with no timeout can hang the whole job until the runner's 6h limit — no report, no teardown.** GAP-DA's migrate-binary smoke probe (`docker run`, not `-d`) would hang forever if the native binary deadlocked in a DB/native-init path — ironically the exact regression the probe exists to catch. Bound the command (`timeout <s> docker run …` → a hang becomes a prompt exit-124 gate failure; force `docker rm -f` cleanup) AND add `timeout-minutes:` to the job as a safety net (no `ci.yml` job had one before #338). Reviewer (Codex) caught it as P2. | `.claude/skills/github-ci-health` (§7 pitfall) |
| 2026-07-24 | #350 | **Unwrapping a guaranteed-`Right` under a pre-guard is a *dead branch* that quietly lowers coverage.** When a caller pre-guards the exact condition an `Either`-returning op raises on (an aggregate invariant mirrored at the call site), `op().getOrNull() ?: return` is an *uncoverable* branch — JaCoCo drops even above the gate and no test can kill it. Absorb the impossible `Left` inside Arrow with `.onRight { }` (caller stays total, identical behaviour, no dead branch) and pin the totality assumption ("under a satisfied pre-guard, never `Left`") as a **test**, not a comment. Applied mid-PR on a harness suggestion (no persisted `[claude]`/Codex thread — Copilot was quota-limited, so the signal came from the second commit + the diff). | `.claude/rules/kotlin-quality.md` (pitfalls) |
| 2026-07-24 | #353 | **A default-valued parameter that reads an effect (`= Instant.now()`, `= UUID.randomUUID()`) is a *hidden* clock/RNG — the signature looks edge-injectable but the no-arg path stays impure.** GAP-DK made `SimulationEngine.runDay`'s **clock path** referentially transparent (`now` became an input, not a hidden effect — the whole function is not yet RT, see the corollary) by injecting `now: Instant` from the edge (a Koin `Clock`) and **removing** the `= Instant.now()` defaults on `DomainEvent.occurredAt` and `Audit.now/touch` — making the hidden clock illegal-state-unrepresentable. Corollary: injecting *one* effect (clock) doesn't make a function deterministic while *another* remains (`UUID.randomUUID` on `Movement`/`DailySnapshot`/`FlowMetrics` IDs — a separate identity concern, GAP-DJ/DL); verify the whole effect set and scope the test claim to exactly what became pure. The general "effects at the edge" principle was already in the skill; this is the non-obvious default-argument corollary. | `.claude/skills/fp-oo-kotlin` (Sinais de Alerta + checklist) |
| 2026-07-24 | #353 | **A signature-change ripple can be absorbed with a *test-only* lower-arity overload delegating to the new member — safe only under two conditions.** GAP-DK's `runDay` gained `now`; a test `internal` 3-arg overload delegating to the 4-arg member with `Instant.EPOCH` kept ~55 behavior call-sites untouched. Footgun unless: (1) the overload has **distinct** (lower) arity — with the *same* arity the production **member wins by precedence**, so the overload is silently shadowed and never intercepts (dead, not recursive); and (2) the **full-arity member is exercised directly** in a dedicated test so the new parameter is actually covered. Keep it in `*TestSupport.kt`, never `src/main`. Harness rated the pattern acceptable-and-documented; recorded as reusable guidance since signature-changing refactors recur across the `/fp-oo-kotlin` arc. | `.claude/rules/testing.md` (Known Pitfalls) |
| 2026-07-24 | #348–#350 | **A type in an exported signature must reach each consumer's compile classpath — `api` when reached transitively, `implementation` when every consumer already depends on the source directly.** After ADR-0044 the domain aggregates return `Either<KanbanError, _>`; `arrow-core` was `implementation`, hiding `Either` from consumers that reach it *through* `domain-kanban` → `api` (as `usecases` already does). Caveat (Codex P2 on #352): the *project* deps stay `implementation` on purpose even though `Board.audit: Audit`/`Scenario.board: Board` are cross-module public — every consumer declares the domain modules directly (`ProjectDependencyGraphTest` asserts the edges), so nothing is transitive. Operative rule, not "all exported-type deps are `api`". | `.claude/rules/architecture.md` (Known Pitfalls) |
| 2026-07-25 | #355 | **Refining a *persisted/serialized* field's type with a value-class smart constructor makes legacy data flow into its `require` — one invalid historical value makes the whole aggregate un-loadable.** GAP-DH's `NonBlankTitle` on `Decision.AddItem.title` was safe on the *new* input edge (DTO + `Card.init`), but `Decision.AddItem` had **no** guard before this PR, so a pre-GAP-DH blob could hold `ADD_ITEM {"title":""}` in `stateJson` (the simulation's `decisions` — not `DailySnapshot`, which serializes only metrics/movements). The decode (`DecisionSurrogate.toDomain`) threw straight into `NonBlankTitle(...)` → `Either.catch` → `PersistenceError` → `findById`/`findAll` of the **entire simulation** 500s, not just the field. The new entry edge is easy to remember; the **decode of persisted history** is the blind spot. Fix (`de596cf`): `decodeTitle()` coerces legacy blank to a `"(untitled)"` sentinel + a legacy-decode test — no data migration, history stays readable. Codex rated P1, this harness P2 — same finding. | `.claude/skills/fp-oo-kotlin` (value-class-on-serialized-field callout + Sinais de Alerta row), `.claude/rules/migrations.md` (legacy-tolerant decode section), `.claude/agents/pr-harness.md` (§2.5 checklist) |
| 2026-07-25 | #359 | **Um skill/doc que cita API do domínio em tempo presente deve ter cada identificador grep-verificado no SHA — nome exato, singular vs plural, membro vs extensão.** O enriquecimento OOD do `/fp-oo-kotlin` ensinava `simulation.advanceDay().appendDecisions(decisions)…`, mas só existe `appendDecision` (singular); e mostrava `Card.block` como extensão quando é método membro. Uma diferença de um caractere "compila na cabeça" do autor/revisor e gera exemplo não-compilável — a leitura de prosa não pega. Harness P2/P3 no #359. | `.claude/agents/pr-harness.md` (§2.5 — bullet skill/doc) |
| 2026-07-25 | #360 | **Um refactor tell-don't-ask "behavior-preserving" que troca um append de LOTE por um `fold` do append SINGULAR troca O(n) por O(n²) em silêncio.** Ao mover a transição de lote do call-site para dentro do agregado, o engine passou a dobrar `appendDecision` (singular) num `fold` — cada iteração recopiava a lista de decisões acumulada (O(atual×lote + lote²)), regredindo o antigo append de lote único; comportamento idêntico, testes verdes. Regra: ao encapsular, dê ao agregado um método de **lote** (`appendDecisions(List)` = uma concatenação), não itere o singular. Ironia colhida: a própria seção OOD do `/fp-oo-kotlin` (adicionada no #359) exibia o padrão `fold(appendDecision)` como exemplo ✅ — corrigido junto. Codex P2 no #360; corrigido com `Simulation.appendDecisions`. | `.claude/skills/fp-oo-kotlin` (exemplo Tell-Don't-Ask + callout de complexidade), `.claude/agents/pr-harness.md` (§2.5 checklist) |
| 2026-07-25 | #364 | **O gate de revisão pode ficar silenciosamente VAZIO — ausência de parecer não é aprovação.** O #364 (implementação real, `src/main`) foi mergeado com **zero revisão** e nada vermelho: Codex não comentou, Copilot recusou por quota e o harness **rodou e falhou**. O run resolveu o PR certo (`Resolved PR #364 — head … d976afa7`) e a `claude-code-action` retornou `is_error: true` com `num_turns: 1`, `duration_ms: 301`, `total_cost_usd: 0` — a mesma assinatura do GAP-CW, agora já com `--model claude-sonnet-5` fixado (logo, causa-raiz distinta: acesso/validade do modelo, não o sufixo `[1m]`). Como o passo é `continue-on-error` (advisory por design), o run concluiu **verde** e o único sinal foi um `::warning` na aba Actions — nenhum comentário no PR. Família do "✅ fabricado" do GAP-CC: silêncio lido como aprovação. Regra: **antes de dar o PR como pronto, confirmar que o parecer EXISTE para o head SHA**; se não existe, checar o run por `is_error` e dispatchar manual. Armadilha de diagnóstico: em `workflow_run` o `head_sha` exposto para os runs de `pr-review.yml` é o da **default branch**, então filtrar por SHA do PR dá falso "nunca rodou" — case por horário e confirme `Resolved PR #<n>` no log. Visibilidade no PR + causa-raiz do `is_error` = **GAP-EK** (mudança de gate/CI, não emendável aqui). **Não é pontual:** o mesmo `is_error: true` aparece nos runs de #363, #364 e #365 — o gate está vazio há pelo menos três PRs. | `.claude/skills/pr-review/SKILL.md` (callout "ausência de parecer não é aprovação" + verificação), Board #6 `GAP-EK [N]` |
| 2026-07-25 | #365 | **"Existe um parecer no PR" não é o mesmo que "existe um parecer PARA ESTE head" — sem SHA no report, um parecer stale faz o head novo passar por revisado.** A verificação escrita no #364 lia o head SHA e nunca o usava: contava comentários `## PR Review Harness` no PR inteiro, então num PR que recebeu novo push depois de revisado o contador seguia > 0 e declarava revisado um commit que ninguém olhou — o próprio falso-verde que o callout existia para impedir, um nível acima. Os **inline** já ancoravam no SHA (`<!-- pr-harness:<sha>:… -->`); o **report-resumo** não tinha marcador nenhum. Fix: o rubric passa a abrir o report com `<!-- pr-harness-report:${REVIEWED} -->` e a verificação filtra por esse marcador. Pegadinha de ferramenta encontrada ao validar: `gh api --jq` aceita **um** argumento — `--arg` falha com `accepts 1 arg(s), received 4`; interpole o SHA na expressão. Codex P2. | `.claude/agents/pr-harness.md` (§5 — marcador de SHA no report), `.claude/skills/pr-review/SKILL.md` (filtro por SHA + nota do `--arg`) |
| 2026-07-25 | #365 | **Um formato exigido pelo rubric só vale se TODOS os caminhos de emissão o exigirem — o prompt do CI é um segundo autor do artefato.** A correção acima passou a mandar o report abrir com `<!-- pr-harness-report:<sha> -->` no `.claude/agents/pr-harness.md`, mas o prompt do `.github/workflows/pr-review.yml` continuava pedindo só *"começando com o título exato `## PR Review Harness`"*. Como o CI emite quase todo parecer, a verificação daria falso "sem parecer" **justamente no caminho default** — e um check que erra sempre volta a ser ignorado, que é o mecanismo do #364 um nível acima. Regra: ao mudar o formato de um artefato produzido por agente, altere o rubric **e** todo prompt/workflow que o instrui, no mesmo PR. | `.github/workflows/pr-review.yml` (prompt exige o marcador), `.claude/agents/pr-harness.md` (§5 — ordem explícita + ponteiro recíproco ao workflow) |
| 2026-07-25 | #365 | **Ao verificar "o gate rodou?", enumere TODOS os estados terminais — com `continue-on-error` há três, e dois concluem verde.** O grep de diagnóstico cobria parecer-postado e `is_error`, mas não o run que **nem chega** ao harness (`Nenhum PR aberto com head == $RUN_SHA … Pulando`, ou job `skipped`) — verificado no run `30171786595`. Pior: `gh run view --json jobs` reporta o passo como `conclusion: success` mesmo com `is_error: true`, então a checagem "barata" por JSON reintroduz o falso-verde que o bloco existia para matar — só o `--log` distingue. Mesma família do "✅ fabricado" (GAP-CC) e do guard de `gh api` vazio. | `.claude/skills/pr-review/SKILL.md` (três estados + proibição do atalho por JSON) |
| 2026-07-25 | #367 | **Um predicado emprestado carrega a direção-segura do problema ORIGINAL — reusá-lo noutra decisão inverte o fail-safe.** A regra de "pode mergear sem revisão?" foi escrita reusando o `*/src/main/**` do guard anti-loop do `post-merge-harvester`. Lá, errar para "não é implementação" custa uma lição não colhida (barato); aqui, custa **mergear sem revisão** um PR que muda produção. E a exclusão vaza justamente o que não tem `src/main`: `Dockerfile`, `k8s/**`, `.github/workflows/**`, `build.gradle.kts`, `buildSrc/**`, `config/**`, `scripts/**` (consumidos pelos gates), migration Flyway. O #367 **se auto-isentaria** ao alterar `.github/workflows/pr-review.yml`. Regra: decisão de risco usa **allowlist** do que é dispensável, nunca blocklist do que é arriscado — o caminho não-reconhecido tem de cair no lado seguro. E ao emprestar um predicado, verifique se a direção-segura dele coincide com a do novo uso. Codex P2. | `.claude/skills/pr-review/SKILL.md` (allowlist executável + porquê), `.claude/agents/post-merge-harvester.md` (aviso de não-reuso do predicado) |
| 2026-07-25 | #367 | **Um snippet de DECISÃO dentro de uma skill é código de gate — `cmd \| grep -v …` engole exit≠0 e entrega stdout vazio.** A allowlist recém-criada (linha acima) lia `gh pr view … \| grep -vE …`; com token expirado, rate limit ou PR inexistente o `gh` devolve **stdout vazio e exit 1**, indistinguível de "doc-only" — e como vazio significava "isento", a regra criada para impedir merge sem revisão passava a **autorizá-lo**. Modo `gh api` exit-0-corpo-vazio do GAP-CC/#288, agora dentro do próprio guard. Regra: capturar em variável, tratar o `||`, e distinguir **vazio** de **falhou** (vazio ⇒ indeterminado ⇒ lado seguro). | `.claude/skills/pr-review/SKILL.md` (comando endurecido), `.claude/agents/pr-harness.md` (§2.5 — snippet de decisão em skill conta como gate) |
| 2026-07-25 | #367 | **Allowlist por diretório vaza; a granularidade tem de ser o TIPO de arquivo.** `\.claude/` inteiro isentava `hooks/guard-security.sh` (o guard de segredo hardcoded que a própria `security.md` declara) e `settings.json`; `[^/]+/src/test/` isentava `architecture/src/test/**` — o módulo **test-only** das 15 fitness functions, onde apagar `ProjectDependencyGraphTest.kt` deixa `testAll`/JaCoCo/PITest **verdes** porque não há `src/main` para cobrir. Fix: só `**/*.md` nesses diretórios e **enumerar** os módulos de produto cujo `src/test` é isento, deixando `architecture` fora. Verificado caso a caso antes de commitar. Harness P2. | `.claude/skills/pr-review/SKILL.md` (allowlist por tipo) |
| 2026-07-25 | #368 | **Um guard cuja allowlist inclui os arquivos que o definem autoriza a própria remoção.** A allowlist isentava `.claude/**/*.md` inteiro — ou seja, `skills/pr-review/SKILL.md` (a decisão de isenção), `agents/pr-harness.md` (o rubric), `rules/security.md` (o que é proibido) e `docs/politicas-explicitas.md` (quality gates, regras de ADR). Um PR que reabrisse o pipe fail-open ou afrouxasse o rubric sairia como "doc puro" e mergearia sem revisão. Não é hipotético: aplicada a deny-list, o **#365 passa de EXEMPT a REVIEW-REQUIRED** — e ele de fato mergeou sem revisão alterando o rubric. Regra: ao escrever um guard, rode-o **contra o próprio diff que o introduz e contra os arquivos que o definem**; se ele se isenta, está errado. Codex P1. | `scripts/review-exemption.sh` (deny-list `GATE`), `.claude/skills/pr-review/SKILL.md` |
| 2026-07-25 | #368 | **Snippet canônico com variável nunca atribuída publica vazio (ou aborta sob `nounset`).** O §5.5 mandava `printf '%s\n' "$PARECER"`, mas `PARECER` não era definido em lugar nenhum do rubric: o comando postaria **só o marcador de SHA**, sem parecer — falha silenciosa no exato artefato que a sessão passou a exigir. Corrigido para heredoc **com aspas** (`<<'EOF'`), que também protege crases/`$`/`!` do markdown do próprio parecer. Regra: snippet canônico é executado por quem lê — toda variável usada tem de ser atribuída **no snippet**, e ele deve ser rodado antes de virar cânone. Codex P2. | `.claude/agents/pr-harness.md` (§5.5 — heredoc citado) |
| 2026-07-25 | #368 | **Enumerar "os subdiretórios perigosos" vaza; proteja o diretório inteiro.** A primeira deny-list listava `.claude/{agents,rules,skills/pr-review}` — e deixou de fora `CLAUDE.md`, a skill `/github-ci-health` (que documenta o próprio sintoma de gate vazio) e `/xp-kanban`, para a qual a regra *protegida* `rules/workflow.md` **delega** o Board Protocol: bypass por indireção, o caso mais sutil. O PR que introduziu a deny-list alterava `/github-ci-health` e o próprio script o chamava de EXEMPT. Correção: `.claude/**` e `adr/**` saem da allowlist inteiros — skill nova entra protegida por padrão, em vez de depender de alguém lembrar de adicioná-la. Regra geral: numa deny-list de segurança, o default do diretório é *proteger*; a exceção é que se enumera. Harness P2. | `scripts/review-exemption.sh`, `.claude/skills/pr-review/SKILL.md` |
| 2026-07-25 | #368 | **`mktemp -t <prefixo>` é BSD; no GNU coreutils aborta — e o runner é `ubuntu-latest`.** O snippet canônico do §5.5 (o mesmo que já tinha o bug do `$PARECER`) usava `mktemp -t pr-harness-report`: no macOS funciona, no runner dá `too few X's in template` → `$BODY` vazio → redirect falha → **parecer não postado**, com o run verde por `continue-on-error`. Dois defeitos no mesmo snippet em rodadas seguidas mostram o padrão: **snippet canônico tem de ser executado no ambiente-alvo**, não só lido — e "funciona na minha máquina" é especialmente traiçoeiro em utilitário POSIX com divergência BSD/GNU. Fix: `mktemp "${TMPDIR:-/tmp}/pr-harness-report.XXXXXX"`. Harness P2. | `.claude/agents/pr-harness.md` (§5.5) |
| 2026-07-25 | #366 | **Um branch `is` como ÚLTIMO arm de um `when` exaustivo sobre sealed é *partial* — o caminho falso do type-check é inalcançável.** Mesma família do dead branch do #350, mecanismo distinto: o último `is X ->` ainda emite o check, mas o tipo garante que o valor já casou antes, então nenhum teste fecha o arm falso — JaCoCo marca partial e o `codecov/patch` reprova um diff totalmente testado (2 partials novos em `SimulationEngine`/`RunDayUseCase`). Fix: a variante inerte vira o **primeiro** branch — ambos os caminhos passam a ser exercitados, semântica idêntica por ser no-op, com comentário no código para ninguém "arrumar" movendo de volta ao fim. Aparece na conversão **enum → sum type** (enum vira switch e não mostra partial), o que a fila de cards torna iminente (GAP-DL `Movement`/`ExecutionResult`, GAP-DI `DecisionRequest`). Corolário de método: **atribua partials medindo base-vs-head** — a main já tinha 1 pré-existente e só o delta era do PR. Contexto do sinal: Codex e harness não comentaram e o Copilot recusou por quota, então quem pegou foi o `codecov/patch` — gate não-humano. | `.claude/rules/kotlin-quality.md` (pitfalls) |
| 2026-07-25 | #366 | **Decode tolerante que DESCARTA o registro ilegível não é degradação de leitura — é deleção, porque o repo re-serializa o agregado inteiro.** A regra de legado existente cobria *valor inválido em dado antigo* (compat para trás); este é o eixo oposto: **variante/tag que este release não conhece**, e o gatilho realista **não é corrupção, é rollback** — um release grava uma 5ª tag, reverte-se, e os pods antigos não leem o histórico que acabaram de gravar (sob rolling update as versões convivem, então blob misto é o caso normal). Preservar em `Decision.Unknown(type, payload)`/`MovementType.Unknown(raw)` mantém o round-trip byte-idêntico; um `skip` viraria **deleção permanente no próximo save**, já que `JdbcSimulationRepository` faz `it[stateJson] = encode(simulation)` do agregado todo — perda por um caminho que ninguém lê como escrita (vale p/ qualquer blob read-modify-write). Custo da tolerância: o `else` que explodia era o que pegava decoder desatualizado, então uma variante nova passa a virar `Unknown` **em silêncio** — como a tag é `String` o compilador não dá exaustividade no decode, e a garantia tem de ser reposta por um round-trip exhaustiveness test que **para de compilar** ao surgir variante nova. Assimetria a preservar: persistido degrada, **input de cliente segue fail-closed (400)** — não propagar a tolerância para a borda HTTP. | `.claude/rules/migrations.md` (nova seção "Variante (tag) desconhecida no blob") |
| 2026-07-25 | #369 | **"Preserva o registro" é uma afirmação sobre o TIPO do campo portador, não sobre a intenção — e o tipo tipado mente.** A regra recém-escrita dizia que `Decision.Unknown(type, payload)` "volta idêntica ao wire". Medido contra o código mergeado: `payload` é `Map<String,String>`, então um payload futuro com número/boolean/array/objeto lança `JsonDecodingException` **antes** de `Decision.Unknown` existir (= o 500 que a tolerância existia para evitar), e um campo extra no topo da decisão é descartado em silêncio por `ignoreUnknownKeys` (= some no próximo save). Ou seja, a tolerância cobria o eixo *tag desconhecida* e não o eixo *forma do payload*. Regra: preservação real exige reter o **JSON cru** (`JsonElement`), não um mapa tipado; se o carrier tipado bastar, **declare o escopo** ("payload plano de strings") em vez de prometer identidade de wire, e teste os dois casos — que são invisíveis porque só ocorrem com dado *futuro*. Meta-lição: eu escrevi a regra no mesmo ciclo em que implementei o código e **não medi o carrier**; o log já registra duas vezes (#326, #332) o custo de gravar mecanismo mal diagnosticado. Codex P2; lacuna de código cardada como **GAP-EN**. | `.claude/rules/migrations.md` (tabela do que o carrier tipado NÃO preserva), Board #6 `GAP-EN [M]` |
| 2026-07-25 | #369 | **Gravei o MECANISMO errado de novo — e desta vez o certo só apareceu compilando.** A regra dizia "um branch `is` como último arm de `when` exaustivo é scored partial". O harness compilou cada forma em Kotlin 2.4.10 sob JaCoCo e leu os contadores: `is D.Unknown -> Unit` (corpo **vazio**) por último = PARTIAL, mas `is D.Add -> out += "a"` e `is D.Unknown -> out.size` (corpos **não-vazios**) por último = FULL, com o caminho falso igualmente inalcançável. O gatilho é **corpo vazio no último arm**, não o `is`: o corpo vazio deixa o compilador inverter o salto e o fall-through cai no `NoWhenBranchMatchedException`, que o `KotlinWhenFilter` do JaCoCo só reconhece como *alvo*. **Consequência prática:** a prescrição "ordene a inerte primeiro" só vale com **uma** variante inerte — com duas, a segunda volta a ficar por último com corpo vazio e o partial retorna, com o autor tendo seguido a regra. A regra correta é "nenhum arm de corpo vazio pode ser o último". Este é o **terceiro** incidente da mesma forma no log (#326, #332): mecanismo afirmado por leitura, refutado por execução — e nas três vezes quem mediu foi o revisor, não o autor. | `.claude/rules/kotlin-quality.md` (mecanismo medido + tabela + prescrição corrigida) |
